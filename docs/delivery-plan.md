# Delivery Plan

## Current Goal — PLATFORM-LEARNING-001

**Статус: ACTIVE / IN_PROGRESS, 25.09.2026.** Пользователь выбрал целиком «Платформа и клиенты», «Источники и публикация», «Квиз и кейсы», «Глоссарий», «Повторение и прогресс», затем явно включил зависимости AUTH/GAM/PRIV и условный AC-PROG-06. Это 38 ранее незавершённых AC в выбранных областях и зависимостях; уже READY части сохраняются, процент проекта/эпиков в IMPLEMENT не пересчитывается. Основание: поручение в текущем чате, D-18/20/21 в spec.

**Результат:** общий проверяемый учебный контур для владельца PWA и всех пользователей Telegram с проверенной identity: рекурсивно учитываемый приватный корпус с versioned provenance/review; утверждённые теория, термины и кейсы с пояснениями; тематические/random/adaptive попытки и персональное повторение, mastery, прогресс, цели и достижения. Студенческий код/UI PWA и ограниченный demo подготавливаются, но новая регистрация/вход студентов в PWA остаётся DISABLED до отдельного решения Q-09/11. Telegram-доступ и изоляция actor сохраняются. Raw Drive материалы и их полный инвентарь не публикуются в открытом repository или клиентских assets.

**Scope AC:** FND-01/02/03/04/06/07/09/10; SRC-01/02/03/04/05/07/09; QUIZ-02/03/05/08/09; GLO-01/02; PROG-01/02/04/06; AUTH-01/03/05/06/07; GAM-01/02/03; PRIV-01/02/03/04. Существующие READY AC/FND-05/08, SRC-06/08, QUIZ-01/04/06/07, PROG-03/05 и прочие зависимости сохраняются как regression contracts. PRIV-02/03/04 нельзя закрыть готовностью кода: нужны внешние утверждённые решения; actual student launch — non-goal по D-20.

**Baseline локальной работы:** `origin/main` `d376f482502964f72003bc6e56d246ad2344d9d3`, проверен 26.09.2026; PR1 #311 и PR2 #312 merged/delivered, records ниже. Отдельный worktree `C:/Users/wait9/.codex/worktrees/platform-learning-scope/psychology-quiz`, ветка `codex/mixed-glossary-draft` от этого SHA. Исходный dirty worktree `codex/curriculum-progress` сохранён без изменений. Прямой SSH агента UNSET; штатный CD через GitHub Actions.

### Scope, DoD и порядок

| Часть | Самостоятельно поставляемый результат | Gates |
| --- | --- | --- |
| PR1 — общий backend, source gate и тема «Кейсы» | Приватный recursive/incremental source inventory с безопасной агрегированной сверкой, versioned case schema/editorial gate и один отдельно разрешённый авторский кейс в собственной теме; due-list/adaptive/mastery, личные review events/goals/achievements и immutable progress contracts. Новые маршруты/схемы additive, текущие owner/Telegram flows не ломаются. | SQLite + PostgreSQL tests, content approval/selection, migrations/rollback, actor isolation; CI/merge/exact CD до PR2. |
| PR2 — клиенты и отключённый student PWA slice | PWA и Mini App получают доступ к общему API: состояния loading/empty/error, кейсы/повторение/mastery, цели/достижения; React/TS/Vite общий клиентский путь. Student PWA account/auth scaffold проверяется синтетически за выключенным production gate; возрастная и privacy policy ожидают Q-09/11. Demo доступна без аккаунта. Проверенный Telegram initData даёт доступ действующим и новым Telegram users с раздельным личным состоянием. | Client/API/auth/security E2E и browser checks; CI/merge/exact CD, публичный student PWA gate остаётся OFF. |

**Основание разбиения:** Goal затрагивает private source corpus, versioned content, PostgreSQL state, два frontend-клиента и auth/privacy. После отдельного явного разрешения пользователя на один авторский вымышленный кейс и указанный locator публикация этого кейса возвращена в PR1; требование собственной темы «Кейсы» закреплено D-25. PR2 использует доставленный API и контент для клиентских UI и отключённого студенческого среза. Обе части самостоятельно поставляемы; PR1 оставляет существующий продукт рабочим. Узкие задачи оформляются commits; исправления до merge остаются в текущем PR. При необходимости исправимый CD failure допускает hotfix по AGENTS.md.

**DoD:** выбранные AC получают READY только по реализованному поведению и подходящим автоматическим проверкам; частично выполненные или заблокированные AC остаются IN_PROGRESS/BLOCKED с причиной и Evidence. Каждый основной PR проходит local validation, self-review, required CI/review, merge и applicable exact-revision CD с сохранением PostgreSQL/user state. Проверены обе поверхности клиента и закрытый student gate. Goal не объявляется DONE при незакрытых обязательных AC; для Q-09/11 фиксируются внешние blockers без имитации правового решения. После поставки очистить только свои безопасно удаляемые ветки/worktree.

**Non-goals:** фактическое открытие student access, публикация raw Drive files, изменение Drive originals, автоматическое утверждение источников/кейсов, runtime LLM generation, личный Obsidian/RAG/search/pgvector, новые книжные provider integrations и внешний voice-контур. Не выполнять реальные production quiz/reset/mail или изменение пользовательского state ради тестирования. SLO и юридическую policy не выдумывать.

### Validation Plan

| AC/риск | Проверка / ожидаемый результат | Команда/tool, cwd, environment | Этап / обязательность |
| --- | --- | --- | --- |
| SRC-01–09, provenance/privacy | Рекурсивный Drive snapshot с pagination/count/revision и representative readback; new/changed/unchanged, conflict и stale approval fail-closed. Клиентский API/assets не раскрывают originals; один явно разрешённый case derivative проходит review точной редакции. | Google Drive connector read-only для private inventory; `python scripts/source_inventory_report.py --current data/source-inventory-current.json` при наличии приватного export; content validators из README, root; synthetic corpus fixtures | REQUIRED PR1 для inventory/gate и одобренного кейса, PR2 для client-boundary regression; недоступный источник не объявлять утверждённым |
| QUIZ/GLO/PROG, versioned state | Separate/mixed/random/adaptive, explanation, due/empty/edition/reset, mastery 1/3/7 дней, retry/conflict/concurrency/actor isolation; legacy compatibility и сохранность user state. | `python -m pytest -q` для affected suites, root, synthetic SQLite; `tests/postgres` в CI с PostgreSQL 18.6; targeted local tests по diff | REQUIRED PR1 CI; локально affected tests и migration rehearsal по риску |
| AUTH/GAM/PRIV, student PWA gate | Owner/student/anonymous/Telegram access matrix, proof-of-both linking, replay/conflicts, goals/achievements persistence; student PWA gate OFF по default и в deployed config, Telegram users остаются допущены с actor isolation. Deletion policy/preview не определены Q-11 и не могут получить PASS. | Auth/API/integration tests в `python -m pytest -q`, root, synthetic SQLite/PostgreSQL; read-only production gate check | REQUIRED PR2 для реализованного access/gate; Q-09/11 BLOCKED для PWA launch и deletion policy |
| FND-04/09/10, клиенты | PWA/Mini App desktop/mobile: теория/термины/кейсы, объяснения, repetition, progress, goals, empty/loading/error/reload; общая actor identity, demo без user-state writes. | `npm test`, `npm run build`, `npm run test:e2e` в `pwa`; применимые Mini App tests; browser на synthetic owner/student с gate OFF | REQUIRED PR2 CI и targeted local/browser checks |
| PR delivery / recovery | Diff/self-review, exact artifact, migration/backup/isolated restore при изменении state, auth/health/readiness/public smoke и запрещённый student access. | GitHub primary CI/CD records и [VPS runbook](miniapp-deployment-qa.md), [PWA delivery](pwa-delivery.md); production read-only smoke | REQUIRED после каждого merge; без speculative rerun |

**E-PL-01 / PR1 #311 delivered, 25.09.2026 19:18 UTC:** squash merge `aef1bc0fc4c18a6848f89153567dd68787dd77fa`; PR CI `36178437860`, main CI `36178917582`, CD `36179233012` / job `108217257608` PASS. Exact artifact `10882943528`, SHA-256 `4f52f79732136b802a380bb67f782803ca47d87a6cf2ba717c2a2362273ec801`; backup/isolated restore, PostgreSQL v3, user state preservation, DB/auth/HTTP/public 11-asset smoke and both running service revisions PASS. Local final Python suite 732 PASS/120 SKIP/24 subtests, validators, 22 PWA component and 32 SQLite E2E PASS. Backend source inventory/editorial gate, one authorized fictional question in topic «Кейсы» (D-25), versioned case feedback, due/adaptive/mastery, queue-backed review events, weekly goals and achievements delivered. Two intermediate CI failures were fixed in the same PR; final checks passed. PR1 branch removed after tree comparison. No production answers or resets. The owner selected a three-item guest demo (theory, glossary, case), without persisted history; student production gate remains OFF. PR2 begins from exact delivered SHA above; next step is client UI, disabled student slice, tests, CI/merge/exact CD.

**E-PL-03 / PR2 delivery checkpoint, 26.09.2026:** PR2 #312 merged `d376f482502964f72003bc6e56d246ad2344d9d3` 20:39 UTC 25.09; final PR CI `36186677008`, main CI `36187032462`, CD `36187428521` / job `108244176030` и Cloudflare production build PASS. Exact SHA, PostgreSQL/HTTP smoke, обе runtime image revisions, `PWA_PUBLIC_SMOKE_OK` (11 assets), `PWA_DELIVERY_OK` и `DEPLOY_OK` подтверждены; production answers/reset не выполнялись. Ветка PR2 удалена, локальный main fast-forward; исходный dirty worktree сохранён. Текущая локальная ветка `codex/mixed-glossary-draft` от `d376f48`, без push/PR: Mini App recovery сверяет session+question; PWA/Mini App поддерживают glossary single/mixed/all, server сохраняет per-topic snapshots и reset выбранной темы с сохранением чужих ответов/review. Python affected reset/repetition/mastery/achievements 53 PASS, UI component 3 PASS, PWA/Mini App build PASS; Mini App recovery 8 E2E, mixed glossary PWA 2 E2E и Telegram Mini App 2 mocked-API E2E PASS на desktop/mobile, 26.09.2026. Browser/SQLite tests не заменяют PostgreSQL CI. GLO-01/QUIZ-02 и FND-07 остаются IN_PROGRESS до полного cross-kind/cross-client контракта. На этом checkpoint по коду завершено 13/38 AC Goal, частичные не включены. Следующий шаг — реализовать оставшиеся AC локально; лимит двух основных PR уже использован, публикация нового основного PR требует решения об упаковке работы. Q-09/11 блокируют student PWA launch, Telegram доступен всем verified users.

**Локальное продолжение, 26.09.2026:** без push/PR подготовлена детерминированная проекция опубликованных glossary entries в общий approved quiz bank и parity audit; mixed setup для обычного/adaptive квиза при доступности включает каждый запрошенный вид (теория, термин, кейс). CD-классификация glossary/publication изменений предусматривает backup и seed. Очередь и mastery локально объединяют captured ответы старых glossary sessions и новых quiz attempts по `(topic_id, term_id)`, сохраняя actor/edition boundaries; тест со старой попыткой, двумя новыми, другим пользователем и изменением редакции PASS. PWA/Telegram API integration на linked actor подтверждает общую попытку, ответ, feedback и очередь при изоляции другого actor (2 PASS). Локально 67 проверок projection/parity/publication, 21 quiz/case, 83 затронутых repetition/mastery/API/learning tests и `deploy.sh` syntax PASS. PostgreSQL CI и merge/CD пока PENDING; AC-GLO-01/02 и AC-QUIZ-02 остаются IN_PROGRESS. На этом checkpoint число завершённых по коду AC остаётся 13/38; третий основной PR не открывать без изменения согласованного порядка Goal.

**E-PL-04 / приватный Drive metadata snapshot, 26.09.2026 05:51 UTC:** read-only рекурсивная выдача папки «Психология» через подключённый Google Drive: 92 папки, 413 уникальных файлов (230 PDF, 180 Google Docs, 3 DOCX), без ошибок; максимальная прямая папка содержит 45 элементов при лимите 100, повторная выдача с лимитом 1000 совпала. Все 63 source records текущего `content/source-corpus.json` найдены с тем же `modified_time`; это подтверждает metadata этих записей, не полноту corpus review. Два Google Docs «Практика №45/46» в модуле 6 имеют редакцию от 25.09 и не входят в эти 63 records; текст получен для предварительного анализа, но юридические/этические утверждения из транскриптов не сертифицированы и derivatives не опубликованы. Полный приватный inventory и оригиналы не сохранены в репозитории; повторная сверка revision требует нового чтения Drive. AC-SRC-01–05 остаются IN_PROGRESS.

**E-PL-05 / текущий checkpoint, 26.09.2026:** поставку PR2 повторно сверили по первичным records: #312 merged `d376f482502964f72003bc6e56d246ad2344d9d3`, PR CI `36186677008`, main CI `36187032462`, CD `36187428521` success с PostgreSQL/HTTP smoke, обеими runtime revisions и 11 PWA assets. AC-FND-09 теперь READY по доставленным desktop/mobile PWA quiz/glossary/case, demo, progress/goals/literature и проверкам обоих DB backend; student PWA gate остаётся OFF по D-20. Реализовано по коду 14/38 AC текущей Goal. Локально без push/PR добавлена команда `/literature` и меню чтения для Telegram-чата через общий `literature_service`: связанный actor видит запись из PWA/Mini App, другой Telegram user изолирован, устаревшие callbacks fail-closed; 92 targeted chat/web/menu regression PASS в `.venv` на synthetic SQLite. AC-FND-10 остаётся IN_PROGRESS до проверки всего cross-client quiz/literature контракта и CI/merge/CD локального изменения. Системный `python` не смог собрать tests из-за несовместимых pydantic/pydantic-core; проектная `.venv` работает. Следующий шаг — продолжить оставшиеся AC локально и подтвердить PostgreSQL контракт; лимит двух основных PR исчерпан, третий основной PR не открывать без решения пользователя.

**E-PL-06 / локальное исправление F-035, 26.09.2026:** тест воспроизвёл 0 достижений после ошибки в старом glossary и верного ответа на тот же термин через shared quiz review. `app/achievements.py` теперь берёт captured history одного термина из `app/repetition.py`, сопоставляет оба вида review-event и выдаёт один `corrected_error` только на ту же опубликованную редакцию; несовпадающая projection не объединяется. Обратное направление quiz → glossary и отрицательная проверка stale projection PASS. Затронутые achievements/repetition/mastery/progress/web suites — 51 PASS на synthetic SQLite; PostgreSQL contract добавлен, локально skipped без сервиса, CI/merge/CD PENDING. Реализовано по коду 14/38 AC Goal: исправление сохраняет уже учтённый AC-GAM-02, нового AC не добавляет.

**E-PL-07 / сверка source graph, 26.09.2026:** сохранён только в ignored `data/` приватный полный metadata export Drive из E-PL-04; `scripts/source_inventory_report.py --reviewed` сверил его с canonical registry/curriculum без публикации ID/названий. Из 413 файлов 63 присутствуют в reviewed source registry и все 63 имеют ту же title/modified_time; 37/37 curriculum lesson edges указывают на точные source revision и snapshot SHA; 12 reviewed learning sources не привязаны к curriculum lesson, 350 файлов ещё вне source registry. Это metadata/graph evidence, не подтверждение содержательного review всего корпуса. Без отдельных `--processed`/`--links` их нулевые/ожидающие сводки не являются оценкой выполненной работы. 13 targeted inventory/curriculum tests PASS на synthetic data, private report PASS на snapshot; CI/merge/CD локального изменения PENDING. AC-FND-01/SRC-01/02/03 остаются IN_PROGRESS, по коду 14/38 AC Goal.


**E-PL-08 / cross-client actor boundary, 26.09.2026:** на synthetic SQLite один связанный owner создаёт и отвечает на попытку через verified Telegram Mini App, PWA видит тот же ответ/ошибку/review queue; второй verified Telegram user не может ответить в чужой session и видит пустые личные review/progress/mastery. Общие published questions доступны обоим, цели, materialized achievements и литература изолированы; PWA и Mini App дают одинаковое состояние владельца. Дополнительно ответ Telegram-чата в PWA/Mini App восстанавливается из той же attempt, чужой actor отклонён, повторный PWA ответ не дублирует запись; 37 затронутых Mini App/chat/literature/progress/concurrency tests PASS; PostgreSQL CI, merge/CD локальной ветки PENDING. AC-FND-02/10 остаются IN_PROGRESS до полного cross-client contract; 14/38 AC по коду.

**E-PL-09 / private-source boundary, 26.09.2026:** на локальной ветке `f3f993f` PWA `npm run build` и Mini App `npm run build:miniapp` PASS; собранные 12 PWA и 3 Mini App файла проверены по 63 source IDs, corpus root ID и маркерам `drive:`/`source_ref(s)`, совпадений нет. Demo/case/literature/glossary/Mini App API negative suites — 64 PASS и 16 subtests PASS на synthetic SQLite. Это локальный asset/response scope, не проверка production artifact или всего частного корпуса; AC-SRC-09 остаётся IN_PROGRESS до полного role/route audit и CI/CD.

**E-PL-10 / приватная source review queue, 26.09.2026:** CLI различает `relocated` при переносе файла между папками с неизменной содержательной metadata и сверяет canonical `corpus_path` с живым путем. Для последовательного source review создана пофайловая очередь только в ignored `data/` с fail-closed проверкой Git ignore и запретом перезаписи; aggregate stdout остаётся без IDs/названий. Readback приватной очереди: 413 файлов, 63 current reviewed metadata/path, 350 untracked, 37 linked и 0 missing tracked; без processed/previous snapshots соответствующие поля unknown, а не «не проверено». 15 inventory/curriculum tests PASS; actual content review, CI/merge/CD PENDING. AC-FND-01/SRC-01/02/03 остаются IN_PROGRESS, 14/38 AC Goal по коду.

**E-PL-11 / восстановление последнего ответа, 26.09.2026:** локальный diff после `ed79b39` добавляет `recent_answer_question` из immutable attempt snapshot в `/web/quiz/state` и `/miniapp/state`; PWA/Mini App показывают текст сохранённой редакции при восстановлении разбора вместо общего заголовка. В API не выдаётся `source_ref`. Тест проверяет ответ перед следующим вопросом, изменение опубликованной редакции, восстановление контекста кейса и одинаковый readback связанного actor в PWA/Mini App; 11 затронутых Python tests и 9 React component tests PASS, обе Vite/TypeScript сборки PASS. Старый API без нового поля сохраняет безопасный общий заголовок. PostgreSQL CI/merge/CD PENDING; AC-FND-07 остаётся IN_PROGRESS, 14/38 AC по коду.

## Завершённая Goal — QUIZ-QUALITY-ANALYTICS-001

**Статус: DONE, 25.09.2026.** Исторический план ниже описывает промежуточные checkpoints; окончательный результат: PR #306/#309/#310 merged, main `8b0909a321314eb4c42c54253df31c796e07351f`, main CI `36164570260` и CD `36164926961` PASS. QUIZ-06, PROG-03 и QUIZ-04 доставлены; связанные прежние PENDING/IN_PROGRESS ниже относятся к состоянию до финального hotfix и не задают текущую работу. Иерархия дисциплин/тем подтверждена D-17; статистическая policy сложности — D-19.

**Результат:** существующие 575 вопросов и 99 терминов получают содержательный source-backed review с точными границами подтверждения; личный прогресс показывает дисциплины и темы с явным знаменателем и ограничениями истории; difficulty не выводится из ошибок одного человека. Сохранённые ответы/редакции и IDs не переписываются.

**Baseline PR2:** `origin/main` `2d90525a665b50531de37bce40ad3573ed70f81c`, после docs #308; PR CI `36155295414`, main CI `36155715009`, CD `36156062047` PASS. Source VPS `2d90525`, backend runtime `c020678`, PWA assets `6be000e` (docs-only). Ветка `codex/quiz-analytics-final` в отдельном worktree создана от этого SHA; исходный dirty `codex/curriculum-progress` сохранён без изменений. PR2 ещё не открыт. Прямого SSH агента нет; delivery через existing CD. Исходный baseline PR1 `6be000e` и его поставка — E-QA-01/02 ниже.

### Scope, DoD и порядок

| ID | Результат / критерий закрытия | Состояние |
| --- | --- | --- |
| G-QA-01 | Каждый существующий question/glossary item рассмотрен по смыслу, ключу/определению, explanation, ambiguity, duplicates и источникам. Компактный ledger связывает exact item revision с доступными source revisions/locators либо явно фиксирует unsupported/disputed. Автоматическая эвристика не подменяет содержательный review. QUIZ-06, F-007/008 | READY, #306 delivered; E-QA-01/02 |
| G-QA-02 | Подтверждённые ошибки исправлены в пределах проверенного материала либо явно отмечены как спорные; publication gate, stable IDs и snapshots сохранены. Ни один unread/недоступный source не выдаётся за подтверждённый. Regression cases проверяют выявленные ошибки и stale/missing review | READY, #306 delivered; E-QA-01/02 |
| G-QA-03 | История, accuracy и дневная динамика доступны по дисциплинам/темам из корпуса; неподтверждённая историческая классификация отделена. Actor isolation, partial attempts, переиздание и недостаток истории не искажают знаменатель и не объявляют mastery. PROG-03 | READY в PR2 code, E-QA-03/04; merge/CD PENDING |
| G-QA-04 | Согласованная policy сложности реализована и воспроизводимо проверена; повторные ответы одного actor и разные редакции не смешиваются в независимую выборку. Optional UX сохраняется. QUIZ-04 | READY в PR2 code, D-19/E-QA-03/04; merge/CD PENDING |
| G-QA-05 | Все содержательные PR прошли applicable checks/review, merge и exact-revision CD с preservation/health/public checks; свои ветки очищены, plan/spec актуальны | IN_PROGRESS; #309 merged, CD post-check failed, narrow hotfix pending |

PR1 source review/ledger и подтверждённые content corrections доставлен (#306). Оставшиеся PROG-03 и QUIZ-04 выполняются одним вторым основным PR: обе части читают immutable answer evidence и допускают общую проверку SQLite/PostgreSQL без частичной поставки. Узкие commits допустимы. DoD: G-QA-01–05 выполнены по D-17/19, неподтверждённые материалы явно перечислены с причиной и необходимым действием, AC не получают READY по одной структуре JSON. Проценты проекта/эпиков не пересчитываются.

**Non-goals:** полный аудит проекта, новые вопросы для расширения покрытия, полный incremental source pipeline, knowledge/search/pgvector, intervals/adaptive scheduling/mastery, новые роли/OAuth, изменение Drive originals, реальные answers/reset/mail и пользовательские данные production во время тестирования. Документальная классификация uncertainty не разрешает массовое удаление банка/истории. Frozen legacy baseline не расширяется.

### Validation Plan

| AC/риск | Проверка / ожидаемый результат | Команда/tool, cwd, environment | Этап / обязательность |
| --- | --- | --- | --- |
| QUIZ-06, truth/provenance | Прямое чтение источников, item-by-item смысл/ключ/объяснение/ambiguity/duplicate review; доступность, revision и locator фиксируются, спорное не скрывается | Google Drive connector, PDF/text review; research вне repository; canonical content validators из README, root | REQUIRED до content approval; unread source блокирует подтверждение зависимого item |
| QUIZ-06, regression/integrity | Exact coverage/fingerprints, missing/stale review, source-vs-bibliography и выявленные семантические дефекты; сохранность IDs/snapshots | `python -m pytest tests/test_content_publication.py tests/test_question_quality_calibration.py tests/test_attempt_content.py -q`, root, synthetic SQLite; новые review cases в affected suites | PR1 PASS; повторять только при влияющих изменениях |
| PROG-03/QUIZ-04 | Actor isolation, partial/history, per-edition independent evidence, verified/unmapped classification, insufficient evidence и denominator | `python -m pytest tests/test_progress.py tests/test_web_progress.py tests/postgres/test_progress_contract.py -q`, root, synthetic SQLite + PostgreSQL 18.6; новые analytics cases | REQUIRED CI; локально точечные tests по изменению/риску, PG contract при изменении queries/transactions |
| QUIZ-04, калибровка | Первый ответ каждого actor/captured edition, раздельные редакции, нулевая/малая выборка и неизменность difficulty metadata; сводка без actor identifiers | `python -m pytest tests/test_difficulty_calibration.py tests/postgres/test_difficulty_calibration_contract.py -q`, root, synthetic SQLite + PostgreSQL 18.6; canonical read-only report command в README | REQUIRED локально для нового поведения и в CI; production только read-only по отдельному безопасному operator шагу, без записи |
| PROG-03/QUIZ-04 UI | Desktop/mobile пустая/короткая история, дисциплина/тема, loading/error/reload; optional quiz UX без regressions | `npm test`, `npm run build`, `npm run test:e2e`, pwa, synthetic backend SQLite + PostgreSQL | REQUIRED CI для затронутых сценариев; локально targeted component/build/browser по риску, production только bounded read-only smoke |
| G-QA-05, delivery | Self-review/diff, актуальные CI/review; exact trusted artifact/runtime, backup/user-state preservation по impact, health/readiness/auth/public smoke | [VPS runbook](miniapp-deployment-qa.md), [PWA delivery](pwa-delivery.md), GitHub primary records | REQUIRED каждый PR; без speculative reruns и реальных progress writes |
| G-QA-05, transport hotfix | При единичном TLS reset public smoke повторяет только запрос, ограниченно; повторная ошибка остаётся failure, содержательная ошибка ответа не маскируется | `python -m pytest tests/test_pwa_smoke.py tests/test_pwa_cd.py tests/test_deploy_production_contract.py -q`, root; Python 3.12, synthetic fixtures; real public smoke в CD | REQUIRED перед hotfix PR и в CI/CD |

**E-QA-01 — source review и regression, 20.09.2026, branch `codex/quiz-source-review`, base `6be000e`, итоговый diff перед PR.** PASS: вручную рассмотрены все 575 вопросов (stems, варианты, ключи, explanations) и 99 терминов (полные/краткие определения, примеры, ссылки), 8 дисциплин. Источники — primary Drive glossary/lecture/practice fragments с exact revisions/locators; 56 сканированных PDF-слайдов качественных методов прочитаны визуально. Ledger: 482 supported, 188 partial, 4 disputed; 12 semantic ambiguity сохраняются явно. Проверка соответствия источнику не является clinical/SME certification или полным прочтением всех 391 файлов корпуса. Raw snapshots/ручные notes остаются вне repo. Исправлены `m1_vnd_002` (контекст учения об условных рефлексах), `dopamine` (нейромедиатор вместо системы) и `m2_exp_040` (границы обобщения единичного случая вместо отрицания экспериментального дизайна по N=1); exact publication reviews отделены от audit ledger. IDs, 575/99 published counts и frozen baseline сохранены.

PASS local: canonical questions/topics/glossary/literature/learning-review validators; `python -m pytest tests/test_learning_reviews.py tests/test_content_publication.py tests/test_question_quality_calibration.py tests/test_attempt_content.py -q` — 89 tests, synthetic SQLite, Windows venv из предыдущей Goal. Проверены stale/missing/orphan review, source vs bibliography, uncertainty, publication fail-closed и immutable attempt editions. Quality metrics остаются 487/575 unique-longest correct и 21 severe cue; это диагностические признаки, не автоматически неверные ответы. Audit quality review: все 674 identities покрыты, evidence отделено от approval, непрочитанное не выдано за проверенное, смысловые пересечения не удалены автоматически; HIGH confidence для охвата/identity, MEDIUM для полноты педагогической интерпретации; remaining concerns — F-007/008. PR #306, первый CI `35536603835`: backend 790 PASS / 1 FAIL — старый glossary runtime test не допускал primary `drive:` ref. Контракт синхронизирован с canonical validator и усилен exact publication evidence; local glossary/publication 77 PASS. Исправление не меняет runtime/policy. Final delivery — E-QA-02.

**E-QA-02 — PR1 delivery, 20.09.2026 21:00:24 UTC:** #306 merged `c0206782af85f059689748974adb94f3b8b3e8ef`; PR CI `35536804625`, main CI `35537028646`, CD `35537191954` / job `106148222767` PASS. Stateful backup/isolated restore, PostgreSQL user-state preservation, readiness/auth и exact bot/API revision PASS. PWA static оставлена на предыдущей revision `6be000e`, 11 assets проверены; source `c020678`. Candidate artifact `10613677341`, digest `4faabd016932686dcedf31d3b236dba7ce23fc42d1554db94fbab0d4e7362d9c`. PR1 branch очищена.

**Checkpoint UPDATE, 25.09.2026:** main/origin/main `d77ab7adec1a6a3b4d9660c3a6086b7cc38ade4f` после docs PR #307; PR CI и main CI `36146756843` PASS. Автоматический CD `36147107055` завершился FAIL на публичном PWA smoke из VPS из-за TLS `ConnectionResetError`; перед этим source sync и прежний runtime `c020678`/PostgreSQL readiness/auth/HTTP smoke прошли. Изменение #307 — только workflow docs, новая runtime-поставка ему не требовалась; повтор CD не запускался, итоговый CD для этой revision не подтверждён. Ветка `codex/curriculum-progress` fast-forward до `d77ab7a`, содержит незакоммиченные код/UI/tests/spec/plan изменения для G-QA-03; PR2 отсутствует. G-QA-01/02 доставлены, G-QA-03 частично реализован без READY, G-QA-04 ждёт policy. Встроенная Goal PAUSED. Следующий шаг только после отдельного IMPLEMENT: сверить dirty diff, завершить PROG-03/QUIZ-04 в одном оставшемся основном PR и проверить его по обновлённому Validation Plan. Проценты не пересчитываются.

**Checkpoint IMPLEMENT, 25.09.2026:** явное возобновление Goal после UPDATE; base PR2 `2d90525`, `codex/quiz-analytics-final` в отдельном worktree. 14 файлов незавершённого PROG-03 перенесены из сохранённой ветки с SHA-256 readback; исходный dirty worktree не изменён. D-19 утверждена. Локально PASS: Python `tests/test_curriculum.py`, `tests/test_progress.py`, `tests/test_web_progress.py` (29), затем `tests/test_difficulty_calibration.py` и `tests/test_curriculum.py` (7); PWA `npm test` (22), typecheck/build, desktop/mobile curriculum E2E (2). Mobile visual review выявил flex-компоновку блока неизвестной темы, исправлено и повторно проверено E2E. Изолированная Python `.venv` использует `requirements-dev.txt`; глобальный Python имел несовместимый `pydantic-core`, его результат не использован. PostgreSQL локально недоступен (`POSTGRES_TEST_DSN` и Docker отсутствуют), required contract — в CI. Следующий шаг: self-review, актуальный diff/validation, commit/PR2, CI включая PostgreSQL, затем merge/CD exact revision и очистка своих веток. Проценты не пересчитываются.

**PR2 CI cycle, 25.09.2026:** #309, head `60a5fad`, CI `36160437517`: backend/real PostgreSQL PASS; PWA SQLite browser 31/32 PASS, один старый mobile assertion выбрал два элемента после появления новой сводки. Селектор уточнён до exact text без ослабления проверки; затронутый desktop/mobile E2E локально 2/2 PASS. Следующий шаг — один сгруппированный push исправления и ожидание актуальных CI/review; первоначальный CI не считается gate нового head.

**PR2 CI correction, 25.09.2026:** head `9b74cc0`, CI `36160917908`: backend/real PostgreSQL снова PASS, PWA SQLite 31/32 — второе ожидание той же подстроки в конце старого mobile test требовало того же exact match. Оба селектора уточнены. Полный локальный SQLite E2E дал 31/32: не связанный с diff glossary click один раз ожидал нестабильный элемент до timeout; последующий точечный повтор glossary и progress на desktop/mobile 4/4 PASS. Повторный CI актуального head должен подтвердить полный контур; без него merge запрещён. PostgreSQL browser stage в обоих failed runs был skipped по зависимости, не N/A.

**E-QA-04 — PR2 code validation, 25.09.2026:** head `96d23ed`, CI `36161555126` / backend `108159084209` / PWA `108159084697` PASS: полный Python behavioral suite включая real PostgreSQL curriculum/calibration contracts, content/parity checks, PWA component/typecheck/build, desktop/mobile browser suite на SQLite и после PostgreSQL import, artifact validation. Локально curriculum/calibration SQLite 7 PASS и progress suites 29 PASS, PWA components 22 PASS, targeted desktop/mobile E2E 4 PASS. `git diff --check` PASS; ревью итогового diff подтвердило отсутствие schema migration, изменения сохранённых попыток и автоматической записи difficulty. AC-PROG-03/QUIZ-04 READY в коде по D-17/19; опубликованная версия и production post-checks ещё PENDING. Последний docs-only commit обновляет Evidence/статусы; required CI его head и merge/CD остаются gates. Проценты не пересчитываются.

**E-QA-05 — PR2 merge и CD recovery, 25.09.2026:** #309 head `a5d0b1a`, PR CI `36162159376` PASS; merge `5b593941fbeeb5a9f27713b59b57e91b2bee7619`, main CI `36162531495` PASS. Автоматический CD `36162899852` FAIL на `scripts/pwa_smoke.py`: `URLError(ConnectionResetError 104)` при public asset TLS из VPS; это не подтверждённая поставка. До ошибки зафиксированы PostgreSQL preflight/smoke, auth 401, internal HTTP smoke и оба running runtime image labels на `5b593941`; внешний read-only `build.json` также вернул `5b593941`. Без automatic rollback и без повторного CD. Hotfix от этого base ограничен bounded retry транспортной ошибки в public smoke; любое несоответствие revision/hash/MIME/header/auth и исчерпанный retry остаются fail. Локальные regression/deploy contracts: 63 PASS, 4 Windows symlink SKIP; CI и новый CD обязательны. Исходная dirty branch сохранена; после hotfix delivery проверить exact runtime/static, затем очистить созданные ветки. Проценты не пересчитываются.

### UPDATE-10 — влияние новой редакции требований

| Область | Изменение AC и действие до реализации |
| --- | --- |
| Клиенты и учебный контент | AC-FND-02/09/10, AC-QUIZ-02/05, AC-GLO-01/02, AC-AUTH-01/03/05 расширены: студенты 18+, общий quiz/glossary/cases, обязательные пояснения, Telegram-литература, PWA demo. Прежнее READY узкого owner-only среза не переносится на расширенный критерий; нужны student role, source/privacy boundaries и cross-client tests. |
| Новый обязательный scope | AC-SRC-09, AC-QUIZ-08/09, AC-LIT-06/07, AC-AUTH-06/07, AC-GAM-01–03, AC-PRIV-01–04 добавлены как самостоятельные критерии. Реализация, data model, миграции целей/достижений, возраст/согласия, UX и tests — в будущих выбранных Goals, не в текущей. |
| Книжные интеграции | AC-LIT-08 (Литрес) — обязательный intent, BLOCKED до Q-10 provider feasibility/условий; AC-LIT-09 (Яндекс Книги) — условный желаемый scope с неподтверждённой реализуемостью. В обоих случаях сохраняется ручной режим. RAG AC-SRH-04/05 теперь только внутренний owner/agent. |
| Исключённый scope | AC-GLO-03, AC-KNW-02, AC-HWK-01/02/03, AC-LIT-05 исключены D-18 без удаления данных/истории и без переиспользования ID. Прежние knowledge/source navigation в AC-QUIZ-05 и AC-GLO-02 заменены пояснениями и приватным provenance. Supporting docs требуют отдельной консолидации F-027. |
| Текущая Goal | AC-QUIZ-06 review существующего банка уже доставлен; AC-PROG-03 и AC-QUIZ-04 по сути не изменились, D-17 сохранено. На момент UPDATE Goal была PAUSED; затем пользователь явно возобновил её, D-19 утвердил. Новые кейсы, студенческий доступ и геймификация не добавлены. |
| Архитектура/delivery | Общая PostgreSQL actor model остаётся; будущие case type, weekly goals/achievements, demo/consent и provider connections потребуют оценки схемы, auth, миграции/совместимости Telegram/PWA и тестов. Public student rollout BLOCKED Q-09/11; книжный sync BLOCKED Q-10. Existing VPS/localization нельзя считать одобренными для этого нового rollout. |

Новая ревизия содержит 86 действующих AC (78 mandatory, 8 conditional), но это **не новый AUDIT и не новая оценка готовности**. Старые проценты ниже относятся только к snapshot 19.09/revision 8. Source evidence — E-UPDATE-10; статус каждой новой/изменённой строки — в реестре AC ниже. Confidence HIGH для текстового diff и трассировки; actual behavior нового scope не проверялся, readiness/feasibility MEDIUM или UNSET по указанным AC.

## Завершённая Goal — LEARNING-CONTENT-001

**Итог: DONE, 20.09.2026 19:14:55 UTC.** PR #301–305 merged/delivered. Итоговый main `6be000ed3f832f3a3def2288a5e54ae3ab917b42`, CI `35531400757` и CD `35531586839` success; exact runtime/PostgreSQL/auth/public assets PASS, artifact/digest указаны в baseline следующей Goal выше. G-LC-01–07 выполнены в согласованных границах; FND-09 остаётся частичным, statistical QUIZ-04 передан новой явно выбранной Goal. Свои ветки удалены, main синхронизирован, отдельный synthetic PostgreSQL остановлен. Далее сохранены исторические checkpoints до поставки, не текущие blockers.

**Статус: ACTIVE / IN_PROGRESS, 20.09.2026.** Основание — прямое поручение пользователя взять AC-FND-08/09, PROG-05, QUIZ-04, SRC-06/07 и LIT-01 как Goal и выполнить реализацию. Встроенная Goal активирована. Уточнение пользователя: FND-09 охватывается только текущими разделами, глоссарием, литературой и сбросом; полный FND-09 остаётся частично выполненным. Поздняя статистическая калибровка QUIZ-04 остаётся Q-03: алгоритм не придумывается; текущий scope закрывает необязательный UX и сохранение difficulty metadata.

**Результат:** самостоятельная PWA получает существующие учебные контуры терминов и литературы; glossary answers/progress переживают перезапуск и согласованы между связанными клиентами; пользователь может явно сбросить своё обучение по теме/целиком. Сложность перестаёт быть обязательным шагом оставшегося quiz UX. Каталог литературы консолидируется по доступному согласованному Drive corpus, а публикация производных материалов сохраняет проверяемое происхождение и отделяет библиографию от доступного знания.

**Baseline:** main/origin/main `2c324bd6c8388f125b596a10e8710b0af3d18cdc`; clean single worktree, branch `codex/learning-reset`, base тот же SHA. GitHub: открытых PR, branch protection и rulesets нет; admin/push доступны. Production: PostgreSQL 18.6, общая actor identity, PWA и backend на baseline, exact-revision CD `35512024023` success. Прямого SSH агента нет; штатная поставка идёт через existing CD.

### Scope, DoD и последовательность PR

| ID | Проверяемый результат / AC | Состояние |
| --- | --- | --- |
| G-LC-01 | Сброс выбранной темы/всего quiz learning требует preview и явного подтверждения актуального состояния. Чужие данные, accounts/linking, literature/private notes и банк сохраняются. Смешанные попытки сохраняют ответы других тем; затронутая активная попытка прекращается, поздний answer/reset не восстанавливает сброшенное и не удаляет новое обучение. PROG-05 | READY, E-LC-01/02/03 |
| G-LC-02 | Glossary sessions, фиксированные варианты, ответы и результат хранятся в БД, общий actor доступен PWA/Telegram; restart/retry/concurrency/foreign-access проверены. После внедрения glossary общий learning reset охватывает и его. FND-08/10, PROG-05, F-018 | READY, #302 delivered, E-LC-03/04 |
| G-LC-03 | PWA desktop/mobile без Telegram открывает glossary quiz и существующий Reading Tracker; empty/error/resume и navigation доступны. Новая литература сохраняет module/topic/source связи и старые user-state IDs. FND-09 (выбранная часть), LIT-01 | READY в PR5 diff, E-LC-09; CI/merge/CD PENDING |
| G-LC-04 | Текущий quiz можно запустить без обязательного выбора сложности, необязательный фильтр сохраняется; существующие setup/reply/callback contracts проверены. QUIZ-04; statistical calibration — ожидается решение | Optional UX READY / #303 delivered; поздняя calibration Q-03 |
| G-LC-05 | Рекурсивно установлен корпус и библиографические источники с pagination/revision evidence; единый каталог связывает повторные упоминания с их источниками, неоднозначные дубли не сливаются автоматически. Только библиографическое присутствие не означает доступность текста/знания. LIT-01, SRC-06 | READY в PR5 diff, E-LC-09; legacy knowledge certification не заявляется |
| G-LC-06 | Новые/изменённые derivative items проходят проверку доступного source и repository approval; новый Drive файл сам не публикуется. Legacy review не объявляется source-certified задним числом; ограничения/спорные записи видимы. SRC-06/07 | READY, #304 delivered / E-LC-07/08 |
| G-LC-07 | Все содержательные PR проходят applicable local/CI checks, self-review, merge и штатный exact-revision CD с bounded post-checks. Plan/spec/contracts актуальны, свои merged ветки очищены; READY выставляется только в выполненной части | IN_PROGRESS: PR1–4 delivered, PR5 local PASS; остаются CI/merge/CD/cleanup |

**Порядок:** PR1 — безопасный quiz reset и его PWA UI; PR2 — durable glossary и PWA, включение терминов в общий reset; PR3 — необязательная difficulty и совместимость Telegram; PR4 — source/publication contract; PR5 — консолидированный литературный каталог и связанный PWA Reading Tracker. Каждый следующий PR начинается после applicable delivery предыдущего. Первый reset явно относится к quiz; полное закрытие PROG-05 требует G-LC-02. Dependencies могут менять границы PR без расширения согласованного scope.

**DoD:** G-LC-01–07 выполнены в текущем scope; поздняя калибровка остаётся открытым Q-03; оба supported DB backend проверены, stateful migrations имеют backup/preservation/recovery gates; реальные источники проверены, отсутствие доступа не подменено N/A. Каждый PR merged/delivered; полный FND-09 не объявляется READY из-за этого ограниченного среза. Проценты не пересчитываются.

**Non-goals:** knowledge/search/pgvector, homework/practice, интервальное расписание/mastery, term cards, OAuth/sharing/new roles, reader/audio capabilities, новая taxonomy статусов literature (LIT-02/Q-04), переписывание Mini App на React, новые infrastructure resources, реальные сбросы или новые ответы production owner во время проверки. Исходники Drive читаются, не изменяются. Источники не заменяются web/LLM или библиографическими названиями книг.

### Validation Plan

| AC/риск | Проверка / ожидаемый результат | Команда/tool, каталог, environment | Этап / обязательность |
| --- | --- | --- | --- |
| PROG-05, integrity | Preview/cancel не меняют learning; confirm очищает только actor/scope; mixed history, stale/retry/concurrent requests, auth/CSRF и rollback сохраняют invariants | `python -m pytest tests/test_progress.py tests/test_web_progress.py tests/test_quiz_service.py tests/postgres/test_progress_contract.py -q`, root, synthetic SQLite + real local PostgreSQL; новые reset cases включаются в те же suites | REQUIRED local до push и CI |
| FND-08/09, glossary | Restart в новом процессе/connection, stable options/feedback, replay/next, actor linking и cross-client isolation | Existing glossary/runtime/API suites и новые real DB contracts; canonical `python -m pytest -q`, root, synthetic state, real PostgreSQL | REQUIRED affected local; полный CI |
| FND-09/PROG-05/LIT-01, UI | Desktop/mobile navigation, glossary/reading/reset, cancel/confirm/empty/error/reload, no private cache | `npm test`, `npm run build`, `npm run test:e2e`, pwa, existing synthetic backend; SQLite и PostgreSQL | REQUIRED affected local и CI; реальный owner smoke только чтение/cancel |
| QUIZ-04, compatibility | Quiz без обязательного difficulty; опциональный выбор и отрицательные setup/reply/callback cases | Existing classic/runner/API tests, root; команды выбранных suites уточняются в содержательном commit | REQUIRED local + CI |
| SRC-06/07/LIT-01 | Полный folder inventory с pagination, чтение source revisions, bibliographic dedup/provenance review; unknown/absent source не получает approval; стабильные IDs | Google Drive connector (read-only), canonical content validators README и новые negative/multi-source fixtures; root | REQUIRED перед публикацией контента; недоступный source блокирует только зависимую запись |
| G-LC-07, delivery | Diff/self-review, required актуальные CI/review, exact main artifact/runtime, preserved data, health/auth/public smoke | README, [VPS runbook](miniapp-deployment-qa.md), [PWA delivery](pwa-delivery.md), GitHub records; production existing target | REQUIRED каждый PR; новые production answers/reset не выполняются |

**E-LC-01, local PASS, 20.09.2026.** PR1 branch `codex/learning-reset`, base `2c324bd`, planning commit `53b0532` + implementation diff: actor-scoped preview/confirm reset, historical-topic selection, partial mixed-attempt preservation, stale/replay protection и PWA UI. SQLite affected suites: 51 PASS; real PostgreSQL 18.6 limited-role progress/auth/business: 51 PASS; дополнительная shared DB/Telegram/API compatibility: 188 PASS + 16 subtests (часть cases пересекается, суммарное уникальное число не заявляется). Frontend: 14 components и typecheck/build PASS, 20 SQLite desktop/mobile scenarios PASS (18 исходно + 2 после уточнения неоднозначного test locator), 20 PostgreSQL browser cases PASS. Desktop/mobile screenshots просмотрены, overflow отсутствует. Первые sandbox runs не смогли создать temporary files/child process; повтор с разрешённым доступом прошёл. Прежний local PG credentials не восстановлены; tests выполнены на новом isolated synthetic cluster, production не затрагивался. Self-review потребовал общей actor lock для legacy setup/finalize, чтобы reset не гонялся с Telegram; abandoned session не финализируется повторно. Новый schema/config/workflow отсутствует; app/db.py вызывает штатные stateful backup/preservation gates при CD. Реальный owner reset не выполнялся.

**E-LC-02, primary records PASS, 20.09.2026 14:01 UTC.** #301 merged в `254ce21c485a05ee0625b7afc91d7433412eea5c`; main CI `35514962675` и CD `35515082509` / job `106089548163` success. Artifact `10606412511`, SHA-256 `d2a0d7b7bc056eaeaa3a42ff17cfcdb200690784a55e3d9dc588452ef63177d8`; backup/isolated restore, POSTGRES_USER_STATE_PRESERVED, оба running image SHA и public smoke 11 assets exact revision PASS. Owner browser после reload восстановил прежние 7 ответов; дополнительный preview/cancel не завершён: автоматическая проверка доступа истекла, при разрешённом повторе прежняя вкладка отсутствовала. Это ограничение ad-hoc Evidence, не failure автоматической suite или CD; production reset не выполнялся. Свои local/remote ветки #301 удалены после сверки одинаковых trees; main синхронизирован.

**E-LC-03, local PASS / Linux gates PENDING, 20.09.2026.** PR2 branch `codex/durable-glossary`, base `254ce21`, migration commit `c692cd1`, shared domain `104b13a` и итоговый UI/docs diff. Additive `postgres-v2`/SQLite `glossary-v1`: фиксированные term/options/definitions, ответы, idempotent transitions и результат в общей БД; Telegram, Mini App и PWA работают через verified actor. Reset охватывает glossary, topic/all isolation и rollback проверены. Полная local regression: 654 PASS + 24 subtests (606 исходно, 46 первоначально получили local PostgreSQL «starting up» после прерывания и прошли отдельно, два дополнительных glossary reset cases PASS). Десять platform skips: Docker Compose 2, Windows symlink privilege 8 — обязательные Linux CI gates. Native PostgreSQL 18.6 recovery v1/v2 dump/isolated restore, migration preservation/idempotence/rollback/drift PASS. PWA: typecheck/build, 19 component/DOM tests и 24 SQLite + 24 PostgreSQL desktop/mobile browser cases PASS; screenshots просмотрены, overflow отсутствует. Mini App DOM regression подтверждает прямое восстановление feedback, fixed options, следующий шаг, topics после завершения без cache и игнорирование позднего setup reply. Self-review сохранил existing Mini App endpoints, auth/no-store/CSRF, общий actor lock, stopped-writer/backup/preservation flow без workflow изменений. Secrets и production learning не менялись. Результаты относятся к synthetic fixtures и указанному branch diff; runtime поставку ещё не подтверждают.

**E-LC-04, PR2 delivery PASS, 20.09.2026 18:00 UTC.** #302 merge `ef9884cfc9a605043536c40080b3cacd24022fef`; PR CI `35527004129`, main CI `35527202232` success. CD `35527356467` attempt 1 / job `106121735231`: backup/isolated restore, PostgreSQL v1→v2, USER_STATE_PRESERVED, strict DB readiness и оба runtime images exact revision PASS; final public TLS handshake получил connection reset. Данные/приложение не откатывались. Независимый public smoke из локальной сети прошёл по trusted artifact `10609938438`, SHA-256 `991af9ad21baaa12aa69b22ffb36686fdd966ce8a605ff8a548ab93096ebf536`. После явного разрешения пользователя один повтор CD (attempt 2 / job `106122539965`) success: HTTP/DB readiness, public 11 assets и PWA_DELIVERY_OK; SOURCE_SYNC_OK/runtime_unchanged exact SHA, без миграции/restart. Свои #302 ветки удалены после tree comparison, main синхронизирован. Первичный network failure не означает дефект schema или потерю данных.

**E-LC-05, PR3 local PASS, 20.09.2026.** Branch `codex/optional-quiz-difficulty`, base `ef9884c`, final diff. 81 affected tests PASS: 47 existing classic/offload/shared quiz cases и 34 real SQLite button→selection/session cases для трёх режимов, optional levels, старых callbacks, malformed values и потерянного selection. Первый запуск ограничен Windows temporary permissions; разрешённый запуск выявил ошибочное имя таблицы только в новом assertion helper, после исправления все 34 cases PASS. Runtime DB/schema/selection алгоритм не меняются. Self-review проверил регистрацию всех callback patterns и сохранение старых сообщений; README отражает новый flow. Required CI/review и automatic delivery PENDING; проценты не пересчитаны.

**E-LC-06, PR3 delivery PASS, 20.09.2026 18:14 UTC.** #303 merge `dabb2808ea93226d207cff3aa8c355b5936887b4`; PR CI `35527999294`, main CI `35528172845`, CD `35528329421` / job `106124319313` success. Backend обеих служб exact merge revision, PostgreSQL readiness/auth boundary PASS; static revision `ef9884c` сохранена, её 11 public assets verified. Artifact current CI `10609879855`, digest `647f84263955d8db69f87ee77a1d7f9ac6f4d0044beb69c2e7177deb7f98ffce`; frontend не менялся. Свои #303 ветки удалены после tree comparison, main синхронизирован. No production answers/reset.

**E-LC-07, PR4 local PASS, 20.09.2026.** Branch `codex/source-publication-gate`, base `dabb280`. 99 unique affected cases + 8 subtests PASS: первоначально 96, затем два positive learning approval cases и CRLF/LF regression; итоговая publication suite 54 PASS после canonical checksum fix. Реальный PostgreSQL 18.6 canonical init/seed/read-only parity: 1 PASS. Три canonical content validators PASS, прежние 575 approved questions / 99 glossary / 42 literature сохранены. Frozen legacy checksum не допускает добавления записи без изменения доверенного кода; live source access/content truth не доказываются JSON validator и требуют review по первичному материалу. 14 bibliography records с проверенными snapshots не объявляют книги доступными learning sources. Schema/DB user state/frontend не меняются; изменение seed script активирует штатные stateful backup/preservation gates CD. Self-review/final whitespace PASS; required CI/review/merge/CD PENDING. Checksum legacy нормализует JSON representation, но изменение данных отклоняется. Raw Drive text/PDF в repository не включены.

**E-LC-08, PR4 delivery PASS, 20.09.2026 18:33 UTC.** #304 merge `fbb070220734b84915da64a8e48e1c22eda9077b`; PR CI `35528997888`, main CI `35529166274`, CD `35529322564` / job `106126996091` success. Native backup/isolated restore record `release-2u_0kykd/record.json`, unchanged seed и POSTGRES_USER_STATE_PRESERVED PASS. Оба runtime exact SHA, PostgreSQL readiness/HTTP/auth PASS; static `ef9884c` и все 11 assets verified. Current CI artifact `10609774384`, SHA-256 `51fba38449067cf7120a3e9d152360e87cd51e7cfde5a079a14134efe670308f`. Свои #304 ветки удалены после tree comparison; main синхронизирован. Full source certification прежнего учебного банка не заявляется.

**Checkpoint / PR5:** `codex/literature-catalog-pwa`, base `fbb070220734b84915da64a8e48e1c22eda9077b`, clean single worktree при старте, open PR отсутствуют, protections/rulesets не заданы. Scope G-LC-03/05, SRC-06 и LIT-01: 130 библиографических упоминаний из 14 прочитанных lists, 114 works после явного duplicate review (15 пар physiology/VND и Алешина counseling/family); module/topic/source/locator и спорные metadata сохраняются. Это grouping библиографии: старые 42 IDs и отдельные per-list reading rows/private notes не объединяются и не удаляются. PWA показывает единый каталог и позволяет выбрать учебный список для существующего Reading Tracker, включая прежние statuses/percent. Topic filter выбирает соответствующую связь; другой topic/source остаётся доступен. Новые library-only topics не создают пустой quiz/glossary. Learning outcomes/reading estimates не выводятся из недоступных книг, bibliography-only явно обозначено. Общая domain persistence для PWA и Telegram, auth/Origin/CSRF/no-store и recovery потерянного ответа. Новая schema, bulk DB/user-state changes, reader/audio/reminders и Q-04 remapping не входят.

Validation REQUIRED local: canonical topics/questions/glossary/literature validators; `python -m pytest tests/test_content_publication.py tests/test_literature_api.py tests/test_literature_catalog.py tests/test_web_literature.py -q` (root, synthetic SQLite), соответствующие real PostgreSQL identity/literature contracts; `npm test`, `npm run build`, `npm run test:e2e` (pwa, synthetic SQLite и PostgreSQL). Проверить 14 sources/130 associations/114 works, 42 stable IDs, named duplicates и ambiguous non-merges, неизвестные author/year без догадок; published exact fingerprints; два actor, no private notes leak, linked cross-client writes, invalid payload/CSRF/rollback; desktop/mobile empty/error/reload/uncertain-save и screenshots. Полная required CI, merge и exact CD с bounded readonly smoke; production reading progress не меняется. До final push обновить AC/findings/checkpoint, общий процент не пересчитывать. FND-09 останется IN_PROGRESS за пределами выбранных разделов; поздняя calibration — Q-03. После delivery закрыть согласованный scope Goal, очистить свои ветки/локальный test service.

**E-LC-09, PR5 local PASS, 20.09.2026 18:57 UTC.** Branch `codex/literature-catalog-pwa`, base `fbb0702`, backend/content commit `1f78e5d` и итоговый PWA/path/docs diff. Основная selected suite: 83 tests + 8 subtests PASS (source gate, каталог, existing Mini App reading API, WebAuth/Origin/CSRF, linked actors, две реальные DB реализации, rollback/private-note preservation). Дополнительно 69 compatibility/path tests + 8 subtests PASS; после explicit ambiguous-title assertions финальные 3 catalog tests PASS. Наборы пересекаются и не суммируются. Четыре canonical validators PASS. Shared persistence оставляет 42 IDs, per-list states/private notes и five legacy statuses без bulk migration; новый grouped response содержит 130 associations/114 works/14 sources/12 topics.

PWA: финальные typecheck/build и 21 component test PASS. Полные desktop/mobile scenarios: SQLite 24 unchanged + 4 reading PASS, PostgreSQL 28 PASS; после финальных UI corrections (статусы 0/100 и повторный вход в раздел) повторены 4 reading scenarios на каждой DB — PASS. Просмотрены desktop/mobile screenshots; горизонтального overflow нет. Browser suite выявила относительные content paths при cwd `pwa`: исправлены module-relative paths с отдельным regression test. Первые новые backend assertions ошибочно предполагали пустую fixture DB; исправлены на проверку сохранения имеющейся legacy row, не удалением fixture. Неизвестные bibliographic metadata не угадывались; source truth проверен по 14 прочитанным спискам, а не по названиям книг. Полнота embedded bibliography всех 391 files и legacy knowledge certification не заявлены.

Self-review: нет schema/config/workflow/dependency изменений; общий actor lock и существующие транзакции, auth/no-store, прежний Mini App response shape сохранены с additive metadata. Новые literature-only topics не попадают в quiz/glossary consumers. Signed-in production writes не выполнялись. Остаточные F-007 (legacy content certification), F-012 (Q-04/форматы), полный FND-09 и поздняя Q-03 остаются открытыми; это не скрытые новые обязательства данного среза.

**E-LC-10, PR5 CI correction / local PASS, 20.09.2026 19:05 UTC.** Первый CI `35530732596`: backend job `106130740892` PASS (771 tests + 24 subtests, actual Linux Compose/native PostgreSQL и validators); PWA job `106130740808` FAIL, logout outside viewport на desktop. После нового пункта меню fixed sidebar без overflow делал нижние controls недоступными; локальный regression на 1280×480 воспроизвёл ту же причину для install control. Исправлены только `overflow-y: auto` и flex shrink sidebar children; тест делает обычные клики установки/выхода, не force-click и не увеличивает timeout. Финальный build/21 components и полные 30 SQLite + 30 PostgreSQL desktop/mobile cases PASS; screenshots просмотрены. Backend/source data с прошлого CI не менялись. F-033 устранён; один сгруппированный push с CSS/test/plan, обязательный CI новой revision впереди. Это code/UI failure, pipeline/gates не изменены.

**Оставшиеся gates PR5:** initial push/PR; все опубликованные CI checks актуального head, review и merge. Затем штатный CD на exact merge SHA/trusted main artifact, PostgreSQL backup/preservation при применимости, running version/readiness/auth и public PWA assets; failed post-check запрещает success, recovery только по действующей процедуре. После подтверждения delivery очистить свою branch, синхронизировать main, остановить отдельный synthetic test PG и закрыть Goal. До этого G-LC-07/Goal остаются IN_PROGRESS; metadata-only follow-up PR и проценты не нужны.

## Завершённая Goal — PROGRESS-001

**Итог: DONE, 20.09.2026.** #299 product и #300 automatic PWA CD merged; final main `2c324bd6c8388f125b596a10e8710b0af3d18cdc`, main CI `35511910042` PASS (624 tests + 24 subtests; 12 components, 16 SQLite + 16 PostgreSQL browser cases), CD `35512024023` / job `106081462696` success 12:57:17 UTC. Trusted artifact `10605496749`, SHA-256 `65c01c6858ec34a28b366c2dd753fe12ec092a229e7bf8313f512b241d0262fa`; backend/PostgreSQL readiness и 11 public assets exact-revision PASS. Signed-in owner read-only smoke: history/statistics/errors/detail, training confirmation cancel и прежний quiz resume PASS; новых production answers/reset/mail не было. F-031 закрыт фактической automatic delivery. Встроенная Goal завершена, свои local/remote branches удалены, main синхронизирован. Ниже — исторические checkpoints до поставки, не текущая незавершённая работа.

**Статус: ACTIVE / IN_PROGRESS, 20.09.2026.** Пользователь ответил «Да, давай» на предложение реализовать личный прогресс и ошибки в PWA после PostgreSQL cutover. Встроенная Goal активирована; это поручение реализации и поставки, не только подготовки плана.

**Продолжение поставки, 20.09.2026:** пользователь поручил «Делай» в ответ на предложение включить публикацию PWA в existing CD. Это разрешает ограниченное изменение pipeline в той же Goal: trusted main CI artifact → проверенный SSH → stage → backend checks → atomic static activation → public smoke. Target, production Environment, SSH credentials, очередь и runtime configuration сохраняются; новые ресурсы, bootstrap и изменение общей access/recovery policy не входят.

**PR поставки:** branch `codex/pwa-cd`, base `771a2780c845dedc75292aac7b17f8b8966658b9`, clean single worktree. #299 merged в этот SHA; main CI `35497051892` и backend CD `35497155646` PASS. CD records подтверждают PostgreSQL 18.6, image/version и HTTP/auth checks. PWA artifact `10600756521` проверен, но static на 20.09.2026 12:03 UTC ещё `1963e76`; ручная активация не выполнялась. Ветка #299 сохранена до завершения поставки. Rulesets, branch protection и production protection rules отсутствуют по свежим GitHub records.

**Дополнение DoD / Validation Plan:** CD использует только artifact успешного последнего main/push CI точной revision, проверяет обязательные jobs, artifact identity/expiry/SHA-256 и manifest; повреждённые, untrusted и stale candidates блокируются. Existing VPS lock охватывает stage/backend/activation; frontend-only changes доставляются без ненужного backend restart, docs-only changes не переключают static. Failure до activation сохраняет прежний pointer; failed post-check запрещает success, automatic rollback/restore нет. REQUIRED local: `python -m pytest tests/test_deploy_production_contract.py tests/test_pwa_artifact.py tests/test_pwa_cd.py tests/test_pwa_release.py tests/test_pwa_smoke.py -q` из root, synthetic Git/SSH/Docker boundaries и temporary artifacts; Bash syntax, YAML/whitespace/self-review. Linux CI обязан выполнить symlink/mode cases, existing suites сохраняются. REQUIRED после merge: штатный CD, exact runtime/static revision, public asset/auth smoke и bounded owner read-only browser checks; повторный ручной bootstrap не нужен.

**Результат:** PWA показывает общую с Telegram историю попыток, результаты, статистику и динамику по существующим темам; можно разобрать сохранённые ошибки и начать тренировку по ним. История использует immutable snapshots, данные выбираются только для проверенного actor.

**Baseline:** clean `main` / `origin/main` `51cecf8ea04062bfdb3f7ff8bfd30b2d813e4e39`; branch `codex/pwa-progress`, base тот же SHA, один worktree. Open PR нет, protections/rulesets отсутствуют. Production PostgreSQL cutover завершён (E-PG-09), public static пока `1963e76`.

### Scope и DoD

| Критерий | Результат / трассировка |
| --- | --- |
| G-PROG-01 | Личная история с постраничной загрузкой, статусом и результатом попытки; подробности содержат только уже сохранённые ответы и их snapshots. Чужая/неизвестная попытка недоступна. AC-PROG-03, FND-08 |
| G-PROG-02 | Статистика показывает числитель/знаменатель по отвеченным вопросам, темы с ошибками и дневную динамику; частичные попытки не выдаются за завершённые, проценты — результат практики, не mastery. Пустая/короткая история объясняется. AC-PROG-03 |
| G-PROG-03 | Ошибки показывают сохранённый выбор, правильный вариант и объяснение соответствующей редакции. Изменённый/retired материал и legacy backfill явно различаются; прежняя успешность не переносится на новую редакцию. AC-PROG-02 (errors), PROG-04, QUIZ-07 |
| G-PROG-04 | Тренировка выбирает только собственные ошибки и approved актуальные вопросы. Пустой набор, активная попытка, изменившееся состояние, двойной запрос и потерянный ответ имеют безопасный исход; новые ответы используют общий quiz runner. AC-PROG-02 (errors), FND-06/07/10 |
| G-PROG-05 | SQLite и реальный PostgreSQL, auth/CSRF/isolation, version/retry/concurrency tests, desktop/mobile UI проходят; PR merged, backend CD и trusted frontend artifact опубликованы, version/health/public/owner checks подтверждены. AC-OPS-01/02/03/05 |

**DoD:** G-PROG-01–05 выполнены, обязательные проверки и поставка подтверждены, canonical contracts/checkpoint обновлены, свои merged branches очищены. Parent AC закрываются только в фактически выполненной части; due-list, version-aware scheduled relearning и отдельная иерархия дисциплин этим срезом не реализуются.

**Non-goals:** интервальное повторение / «На сегодня», mastery, сброс прогресса, glossary persistence, новая taxonomy, новые accounts/permissions, изменения SMTP/DNS/schema, новые инфраструктурные ресурсы и retention policy. Единственное согласованное delivery дополнение — PWA в existing CD. Общее readiness не пересчитывается.

### Реализация и зависимости

Продуктовый PR #299 merged: общий progress domain/API, два PWA экрана и regression suites. Existing `quiz_sessions` / `quiz_answers` / immutable snapshots — единственный источник; новая таблица/миграция не нужна. Следующий PR автоматизирует незавершённую frontend delivery по явному решению пользователя: static из trusted main CI после backend checks. Прямого SSH агента нет; используется существующий GitHub CD SSH, ручная активация владельцем больше не является запланированным gate. Новые answers на реальном owner account для smoke не создавать; write paths проверять на synthetic fixtures.

Политика актуальности ошибок подтверждена пользователем (D-14 spec): последний ответ на ту же редакцию; правильный повтор исключает её из ошибок, прежние редакции сохраняются; интервальная/adaptive policy Q-03 остаётся вне scope. Исторические темы берутся из снимка редакции, а не текущей категории изменённого вопроса. Текущий банк плоский: не выдумывать discipline mapping. Дневная динамика — наблюдённые ответы по UTC, с явными counts и ограниченным периодом отображения; полная история доступна постранично.

### Validation Plan

| AC/риск | Проверка / ожидаемый результат | Команда/tool, каталог, environment | Этап / обязательность |
| --- | --- | --- | --- |
| G-PROG-01–04 | Два actors, пустые/частичные/finished/abandoned attempts, pagination, точные aggregates, original/backfilled/changed/retired snapshots, selection и retry/concurrency | `python -m pytest tests/test_progress.py tests/test_web_progress.py tests/test_quiz_service.py tests/test_web_integration.py tests/test_attempt_content.py tests/postgres/test_progress_contract.py -q`, root, synthetic SQLite и real local PostgreSQL | REQUIRED до push; CI full suite |
| G-PROG-01–04, UI/privacy | Loading/error/empty/detail/retry, navigation и confirmation; no private persistent cache | `npm test`, `npm run build`, `npm run test:e2e`, pwa, existing fixture / desktop+mobile / SQLite+PostgreSQL | REQUIRED local affected suites и CI |
| G-PROG-05 | Whitespace, syntax, self-review, актуальные PR checks/review; exact main CI/artifact и compatible backend | Canonical commands README, GitHub primary records | REQUIRED до merge; без speculative reruns |
| G-PROG-05, stateful runtime | Existing PostgreSQL backup/preservation/health gates, exact runtime revision; static integrity/HTTPS/auth boundaries и bounded signed-in чтение | Existing CD; [static procedure](pwa-delivery.md); production VPS/operator и внутренний browser | REQUIRED после merge; отсутствие доступа не N/A |

### Состояние и Evidence

| Критерии | Статус / оставшееся |
| --- | --- |
| G-PROG-01–04 | READY в main `771a278` (#299): shared history, answered-only detail, accurate totals/daily window, edition/provenance-aware errors, confirmed replacement и replay/CAS protection. E-PROG-01/02 |
| G-PROG-05 | IN_PROGRESS: #299 CI/merge/backend CD PASS; automation code/local checks PASS E-PROG-03, впереди его PR CI/merge/CD, public/owner smoke |

**E-PROG-01, local PASS, 20.09.2026 07:25 UTC.** Branch `codex/pwa-progress`, base `51cecf8`, backend commit `17a7c36` и frontend diff. Python 3.12/psycopg 3.3.6, synthetic SQLite и отдельная PostgreSQL 18.6 DB с limited role: финальный affected suite 90 PASS (plan commands плюс existing PG business regression), без skips. Проверены isolation/CSRF, partial/abandoned/finished/empty counts, pagination, UTC window без усечения totals, original/changed/retired/backfill semantics, actual proof-based linking, concurrent training/replay, restart-style new request/state и сохранённые ответы. PWA: 12 component tests, typecheck/build PASS; полные desktop/mobile browser suites 16 SQLite + 16 PostgreSQL PASS. После удаления duplicate CSS-rule повторены build/components и 6 affected UI/keyboard scenarios PASS; backend/DB code после PG проверки не менялся. Desktop/mobile screenshots просмотрены; новой таблицы, миграции, dependency или workflow нет. Исправлены до push: planned count для незавершённых attempts (session aggregate заполняется только при finalize), mobile navigation overflow и pytest module-name collision. Syntax/docs links/whitespace/self-review PASS. Browser использует synthetic data/mail; production writes/нагрузку/физическую установку эти checks не подтверждают.

**E-PROG-02, primary records PASS/PARTIAL, 20.09.2026.** #299 merged в `771a2780c845dedc75292aac7b17f8b8966658b9`; main CI `35497051892`: Python 578 tests + 24 subtests, PWA 12 component / 16 SQLite + 16 PostgreSQL browser cases и Nginx PASS. Backend CD `35497155646` завершён 07:33:53 UTC: PostgreSQL 18.6 readiness, API/auth smoke и exact running image revisions PASS. Artifact `10600756521`, ZIP digest `71717d3e9738753a0cb90485c42f73084c002c55394c940acc77dce591cb050c`, manifest проверен. Public readback 12:03 UTC ещё `1963e76`: frontend delivery PENDING, повторный backend deploy ради статуса не выполнялся.

**E-PROG-03, local PASS / Linux gates PENDING, 20.09.2026.** Branch `codex/pwa-cd`, base `771a278`, automation diff после planning commit `180cdcf`. Итоговый affected suite из PWA procedure: 104 PASS, 8 Windows symlink-privilege skips (обязательны в Linux CI). Включены trust/run-attempt/expiry/checksum/archive-path/type checks, actual Bash workflow/deploy с fake системными boundaries, source-only/frontend-only/failed-stage/backend/health/new-main/public-smoke failures; enabled PWA обязателен до activation. YAML, все embedded shell steps, deploy/Python syntax PASS. Новый verifier отдельно сверил реальный main CI/artifact E-PROG-02 без внешних записей: PASS. Self-review: SSH verification/permissions/queue прежние, secrets только existing CD; root/config/DB/bootstrap не меняются, backend и PWA остаются под одним lock, old static сохраняется до activation. Полный CI и actual CD ещё не выполнялись; mock checks не выдаются за production Evidence.

**Оставшиеся gates:** initial push и PR automation; все published checks актуальной revision, Linux symlink/mode cases, merge после success. Затем existing CD на exact merge SHA и trusted artifact из его main/push CI; shared lock/main/target/health/public/owner checks обязательны. При failed post-check stop/forward-fix, без automatic DB/static rollback. После delivery удалить свои ветки #299 и automation, безопасно синхронизировать main; metadata-only follow-up PR не требуется.

## Завершённая Goal — POSTGRES-MIGRATION-001

**Итог: DONE, 20.09.2026.** G-PG-01–07 выполнены; #295–298 merged/delivered, operator cutover и owner browser smoke PASS (E-PG-09). AC-FND-05 READY. Встроенная Goal завершена; свои ветки удалены. Ниже сохранены прежние planning/checkpoint сведения с их историческим временем; актуальный результат определяется E-PG-09.

**Статус: ACTIVE / IN_PROGRESS.** Основание: после закрытия PWA-FIRST-001 пользователь 20.09.2026 поручил «Ок, делай» в ответ на предложение переноса PostgreSQL; дополнительно разрешил проверку авторизованной PWA во внутреннем браузере. Встроенная Goal активирована. Это implementation, не повторный аудит; проценты проекта/эпиков не пересчитываются.

**Результат:** bot, Mini App и PWA используют PostgreSQL как единственный runtime store после проверенного cutover; прежние learning actors, accounts, sessions, attempts, answers, snapshots, literature state и content IDs сохраняются. SQLite остаётся неизменённым исходником/точкой восстановления до переключения, а не параллельным writer после него.

**Baseline:** Goal начата с main `391df11ab38122a32dacb18667e69fd4966c7879`. #295–297 merged/delivered на SQLite; текущий проверенный main/runtime `bc878b806ec83590786b7b8e694d231954c41f2d`, public PWA `1963e76`. Исправление storage ownership: branch `codex/postgres-data-ownership`, base `bc878b8`, один worktree, unrelated changes нет; open PR, protections/rulesets перед началом отсутствуют. Operator подтвердил preflight и phase `allocated`; PG контейнер перезапускается из-за прав mount root, cutover не начинался.

### Scope, критерии и DoD

Parent AC: AC-FND-05 и сохранение уже выполненных AUTH-01/02/05, QUIZ-01/02/07, E14; применимые части FND-06/07/08/10 и OPS-01/02/03/04/05. Не закрывать целиком более широкие platform/learning AC результатом одной миграции. F-025 исследовать в части SQL/lifecycle/concurrency, F-014 — в части PostgreSQL backup/cutover/recovery; остальное остаётся в реестре.

| ID | Условие/действие → результат и проверка |
| --- | --- |
| G-PG-01 | Выбран PostgreSQL runtime → все существующие bot/Mini App/PWA auth/quiz/literature операции работают на реальном PostgreSQL; SQLite source/default продолжает работать на подготовительных PR. Schema version/тип конфигурации проверяются явно; неверный DSN не переключает тихо на SQLite. Проверка: обе backend suites, init/seed/parity и API/E2E. |
| G-PG-02 | Мигрируется стабильный SQLite snapshot → IDs, FK/unique constraints, accounts/password hashes/proof/session digests, timestamps, answers/score, immutable snapshot JSON/hash/provenance и literature rows сохранены. Sequence high-water marks не дают повторно использовать прежние IDs. Проверка: row-level reconciliation/fixtures, включая legacy backfill и deleted-ID high-water mark. |
| G-PG-03 | Повтор/ошибка/чужой непустой target → нет destructive overwrite или потери source; частичный import откатывается атомарно, корректный повтор сверяет identity/content. Несоответствие схемы, повреждённый snapshot/FK или drift после snapshot запрещают cutover. Проверка: retry/fault-injection/negative target tests. |
| G-PG-04 | Одновременные setup/answers/token consume/linking/recovery → один согласованный результат, без duplicate score/active attempts/reassignment. Транзакции, locks и конфликтные SQL используют PostgreSQL semantics; connection/timeout ошибки не раскрывают DSN/пароли. Проверка: real PostgreSQL concurrency, auth/replay/isolation/regression и failure cases. |
| G-PG-05 | После миграции выполняется backup/isolated restore → data/schema/sequence reconciliation и application smoke PASS. До первых PG writes допускается согласованное возвращение к неизменённому SQLite; после них автоматический rollback на старый SQLite запрещён, используется stop/forward-fix/проверенное восстановление PG. Проверка: изолированный recovery rehearsal без production restore. |
| G-PG-06 | Поставляется PostgreSQL на установленный VPS → dedicated DB/role/persistent storage, приватный network без публичного DB port, secrets только runtime, preflight/health/version checks. Cutover выполняется под общей delivery lock после остановки всех writers и verified source backup; source сохраняется. Проверка: Compose/config review, operator output и actual post-checks. |
| G-PG-07 | Все PR прошли CI/review/merge, cutover и bounded post-checks → точный runtime version/backend подтверждён; owner quiz/resume/auth и Telegram compatibility не нарушены. Commands/config ownership/runbook/checkpoint обновлены, собственные merged branches/worktrees безопасно очищены. |

**DoD:** G-PG-01–07 выполнены; выбранный runtime действительно PostgreSQL, source SQLite и user-state сохранены; обязательные real-DB/CI/recovery checks PASS, implementation PR merged и applicable delivery подтверждён. Default-off подготовка сама по себе не закрывает Goal.

**Non-goals:** pgvector/search/knowledge, новые learning features, новые users/sharing, переделка UI/Telegram Mini App, удаление SQLite source/backups, production data reset, восстановление всей VPS/соседних проектов, HA/replicas, придуманные SLO/RPO/RTO и автоматическая retention policy.

### Решения и последовательность PR

1. **Storage foundation:** небольшой DB boundary с явным PostgreSQL driver и versioned schema, перенос существующих запросов/transaction semantics, конфигурация с fail-closed selection, import/reconciliation fixtures и real PostgreSQL behavioral tests. SQLite остаётся production default до отдельного cutover; никакого request-time DDL или автоимпорта.
2. **Delivery/recovery:** PostgreSQL backup/isolated restore, deploy preflight/post-check support, dedicated private Compose service и operator cutover tooling/runbook. CI PostgreSQL service обязателен; workflow изменения ограничены проверкой/поставкой этой Goal. Перед ними применяются ci-cd-rules.md.
3. **Cutover:** установленный target и credentials owner, health/storage capacity, прекращение writers, verified backup и schema-compatible import, reconciliation, переключение app config, exact runtime/backend и bounded auth/quiz smoke. Начать следующий PR только после применимой поставки предыдущего. Разбиение можно уточнить по связности без расширения scope.

**Исправление перед cutover (G-PG-06/07):** после успешного preflight операторский блок завершился без prepare/cutover. Проверить и устранить наследование stdin дочерними командами: обычные subprocess получают EOF, явно переданные SQL/manifest bytes и dump stream сохраняются. DoD этого PR: воспроизводимый regression test, affected deployment tests и required CI PASS, merge/обычная SQLite delivery, затем возобновление operator cutover на новой проверенной revision. Новые DB/schema/features и изменение пользовательских данных вне процедуры не входят. Основание — продолжение порученной Goal и присланный оператором failure.

**Исправление bootstrap storage (G-PG-06/07):** оператор на `bc878b8` подтвердил phase `allocated`, root-owned `.postgres/data` mode 0700 и PostgreSQL restart loop с permission denied на `/var/lib/postgresql`. Новый PR `codex/postgres-data-ownership`, base `bc878b806ec83590786b7b8e694d231954c41f2d`: назначить UID/GID 999 только корню собственного private data mount при новой allocation/повторе `allocated`, без recursive chown, смены паролей, очистки или import. Проверять отказ для symlink, чужого owner, широких прав и неожиданной phase. DoD: actual Linux Compose test с production ownership/mode, local guards/deployment tests, CI/merge/SQLite delivery; затем operator resume существующего state и прежние cutover gates. Основание — устранение подтверждённого failure в порученной Goal; продуктовый scope не меняется.

PostgreSQL 18.6 и psycopg 3.3.6 закреплены после проверки официальных packages/images и local validation. CI image 18.6-bookworm закреплён digest; canonical commands, SQL/locking contract и test fixtures — в [PostgreSQL storage](postgres-storage.md). CI service и browser fixture включены в PR1, поскольку проверяют его shared database code; production Compose/cutover остаются PR2. Изменение SQL dialect не означает переписывание domain/API contracts. Порядок блокировок/конфликтов определяется до изменения соответствующих функций и подтверждается concurrency tests; общего выдуманного latency target нет.

### Зависимости и доступ

| ID | Состояние / влияние |
| --- | --- |
| PG-D1 / production target | VPS `167.86.68.98`, `/opt/psychology-quiz`, root через MobaXterm; root account штатного CD подтверждён SSH journal. Прямого SSH агента нет. Latest operator preflight: SQLite 15 519 744 bytes, free 37 398 921 216 bytes при reserve 1 GiB, private `.env` 0600. Phase `allocated` от 20.09.2026 05:52:46 UTC; pinned image UID check=999, DB container restart loop. Mount root uid=0/mode0700, nested PGDATA uid=999/mode0700; permission denied подтверждён log. Bot/API up, SQLite сохраняется. Значения ёмкости — snapshot, повторяются перед prepare. |
| PG-D2 / local test runtime | PASS: PostgreSQL 18.6 native EDB binaries, изолированный loopback-only cluster, Python 3.12/psycopg 3.3.6 и non-superuser test role. Synthetic fixtures; real production history не копировалась. Docker CLI отсутствует; Compose и Linux symlink checks обязательны в CI. |
| PG-D3 / recovery | Источник сохраняется, cutover без dual writers. PostgreSQL dump/restore и reconciliation до включения обязательны; retention/RPO/RTO остаются UNSET и не блокируют bounded migration rehearsal. |
| PG-D4 / signed-in browser | 20.09.2026 инструмент внутреннего браузера стал доступен. Подтверждены сохранённый вход, verified account / Telegram linking, resume на 7/15 ответах, переход к вопросу 8, account view и reload без потери состояния (E-PG-04). Новые ответы/изменение credentials не выполнялись. После PG cutover нужен повторный bounded smoke. |

### Validation Plan

| AC/риск | Проверка / ожидаемый результат | Команда/tool, каталог и environment | Этап / обязательность |
| --- | --- | --- | --- |
| G-PG-01/04, shared compatibility | Existing behavior остаётся прежним; обе БД проходят auth/quiz/literature и restart/concurrency negatives | Canonical `python -m pytest -q` (root, isolated venv); targeted PostgreSQL suite и test target — в [storage procedure](postgres-storage.md#воспроизводимые-проверки), реальный local PostgreSQL/CI service | REQUIRED local affected suite и CI; полный existing suite перед PR1 из-за общего DB boundary |
| G-PG-02/03 | Полная миграция synthetic legacy fixtures, sequence preservation, exact row reconciliation, retry/failure/unknown-target guards | Migration CLI из [storage procedure](postgres-storage.md#команды-хранения), root, synthetic SQLite и отдельная temporary PostgreSQL DB | REQUIRED до initial push и CI; production snapshot rehearsal до cutover |
| G-PG-05/06 | Backup/isolated restore и preflight/cutover failure recovery; no exposed DB/secrets | Existing deployment test harness + PostgreSQL recovery integration; Compose CLI/actual isolated service, root | REQUIRED локально где доступен service и в CI; host gates REQUIRED до изменения runtime |
| G-PG-01/04/07 | Auth/link/quiz/setup/answer/result/reload работают поверх PG; negative access и no private cache/logs | `npm run test:e2e` в pwa с real PostgreSQL backend test fixture; `npm test`/`npm run build` при затронутом frontend/harness | REQUIRED CI; affected local browser suite до push; owner signed-in smoke при доступном инструменте |
| SQL/performance risk F-025 | Измерить representative query plans/повторные подключения/lock contention, не делать вывод о throughput всей production | `EXPLAIN` и bounded synthetic measurements на выбранном PostgreSQL; источник/объём/revision в Evidence | RECOMMENDED до cutover; обнаруженная integrity/timeout regression становится REQUIRED fix |
| G-PG-07, delivery | Exact candidate CI/review/merge, running backend/version, preserved-state, health и bounded owner/Telegram checks | GitHub records, existing AGENTS/VPS procedure, readonly/synthetic preflight и согласованный operator cutover | REQUIRED для каждого применимого PR и финального cutover; не запускать deploy ради статуса |
| G-PG-06/07, operator stdin | Дочерний процесс не потребляет команды caller; explicit bytes/file input доходят без изменения | `python -m pytest tests/test_postgres_vps.py tests/test_deploy_production_contract.py -q`, root, synthetic subprocess/filesystem; existing recovery integration в Linux CI | REQUIRED local перед push и CI; frontend/SQL не меняются, прежние local E2E применимы |
| G-PG-06/07, private storage ownership | Новая allocation и retry назначают только mount root UID/GID 999, mode 0700; неизвестный owner/symlink/phase не исправляются автоматически; реальный PG18 стартует с этими правами | `python -m pytest tests/test_postgres_vps.py tests/test_deploy_production_contract.py tests/test_postgres_compose_runtime.py -q`, root; local synthetic filesystem, actual Docker/Linux в CI | REQUIRED local guards + CI Compose; local Docker отсутствует, skip не является bootstrap PASS; runtime cutover требует operator evidence |

### Состояние PR и Evidence

| Критерии | Состояние / оставшееся |
| --- | --- |
| G-PG-01–04 | Реализованы и проверены в локальном PR diff: обе БД, import/row/sequence preservation, retry/rollback/drift guards, auth/session/token/linking, quiz/concurrency и literature isolation. PR #295 / CI / SQLite CD PASS (E-PG-02); PostgreSQL runtime cutover ещё впереди |
| G-PG-05–06 | IN_PROGRESS: #295–297 merged/delivered, stdin defect F-029 закрыт. Operator preflight PASS; prepare остановлен в `allocated` из-за F-030 (mount ownership). Actual prepare/cutover/reconciliation/post-checks остаются; source SQLite не переключён |
| G-PG-07 | IN_PROGRESS: #295–297 merged/delivered; исправление storage ownership, production PostgreSQL cutover и bounded owner smoke остаются |

**E-PG-01, local PASS / delivery PENDING, 20.09.2026 (UTC 19.09, 21:31–22:06).** Источник — branch `codex/postgres-storage-foundation`, base `391df11`, локальный implementation diff; Python 3.12, PostgreSQL 18.6 native Windows, psycopg 3.3.6, synthetic DB и limited role. Полная Python suite: 492 PASS + 24 subtests, 5 local skips (Docker CLI отсутствует; 4 Linux symlink cases). После review/final changes targeted PostgreSQL/boundary/deployment suite: 55 PASS; Docker/symlink остаются REQUIRED CI. PWA component 8 PASS, typecheck/build PASS; desktop/mobile browser 12 PASS PostgreSQL и 12 PASS SQLite, включая recovery/link/retry/resume/private cache. Compileall и content validators PASS. Real owner browser/session этим не проверены. Final import review добавил table locks и проверку PostgreSQL target до открытия соединения; регрессии PASS. Production backup/restore/capacity/performance ещё не доказаны этим Evidence; F-014/F-025 остаются открытыми. CI services с pinned image проверяются будущим первым run, успех заранее не заявляется.

**E-PG-02, PR1 delivery PASS, 19.09.2026 22:14 UTC.** #295 merge `788426b86e29c7a160aaa08c32b492ac0f0749ab`; PR CI 35472457699 и main CI 35472589929 success, оба jobs и Cloudflare check success; CD 35472680113 / job 105976408177 success. VPS: backup/restore `/data/backups/release-w4x98dn6/quiz.sqlite3`, USER_STATE_PRESERVED, HTTP_SMOKE_OK и обе службы на expected revision. Bot image `sha256:baa7fee037f201e8d10836c32cc3900beb09ce43fba39c2a9e874b6e3ecfb03b`, API image `sha256:c66698007994b7bb8c353fce3eb4ae5c2c3967863b1418dd89662249c7af4792`. PostgreSQL не включался; source/current SQLite сохранён. PR1 local/remote ветки удалены после сравнения tree с merged main.

**E-PG-03, PR2 applicable local PASS / Docker CI and delivery PENDING, 20.09.2026.** Branch `codex/postgres-delivery-recovery`, base `788426b`, final implementation diff. Итоговый affected scope: 157 PASS, 8 subtests; два local skips — Compose CLI и actual private Docker service, REQUIRED Linux CI. Первый запуск дал 154 PASS и один setup timeout после остановки test cluster при прерывании; восстановлен тот же test cluster, единственный не стартовавший concurrency case и добавленные target/image guards прошли отдельно (20 PASS). Actual native 18.6 dump/restore сохраняет все tables/schema/import marker и sequence high-water, damaged archive отклоняется с сохранением source. Проверены cutover interruptions/retry, запрет reimport после writers start, config conflict, shell PG/SQLite gates и readiness failure. Test connection timeout ограничен 10 seconds; runtime semantics не менялись. Production resources не создавались. Frontend/harness не менялись; local E2E E-PG-01 применимы, обе backend browser suites также обязательны в PR CI.

**E-PG-04, authenticated browser PARTIAL PASS, 20.09.2026 около 04:10–04:17 UTC.** Источник — реально открытая вкладка `psy.cloud-nodes.net` во внутреннем браузере пользователя, после появления browser tool. UI показывает verified owner account и связанную Telegram identity, сохранённый последний ответ (7/15), следующий вопрос 8/15, account view и восстановление последнего ответа после reload. Ответы/score/password/logout/mail не изменялись. Static baseline `1963e76`, backend last confirmed CD `788426b`; UI smoke не является отдельным доказательством DB backend или всех auth/write сценариев. Synthetic PostgreSQL E2E E-PG-01 и обязательный CI покрывают эти операции; после production cutover требуется новый bounded runtime/browser check.

**PR2 scope / integration:** G-PG-05–07: native pg_dump/pg_restore с isolated restore и manifest reconciliation; runtime/deployment backend checks; profile `postgres` с отдельной БД/ролью и приватным bind storage без public DB port; operator preflight/bootstrap/cutover с общей lock и durable phase record, сохранением SQLite и остановкой обоих writers. Code targets: service `psych_quiz_postgres`, database `psychology_atlas`, app role `psychology_app`, storage `/opt/psychology-quiz/.postgres/data`; actual host existence/capacity/ownership должны пройти preflight. Bind storage выбран, чтобы default SQLite compose не создавал неиспользуемый named volume; `.postgres/` исключён из Git и Docker build context и не монтируется в app services. Новая privileged setup выполняется root оператором по [процедуре](postgres-storage.md#подготовка-и-cutover-на-vps), не routine CD. Default до переключения — SQLite. При cutover failure оба writers остаются остановленными, retry продолжает сохранённую phase. Перед началом PG writers record фиксирует необратимость автоматического возврата к SQLite; после этой границы import не повторяется. Validation: real native restore/failure tests и shell/backend guards; actual Compose/Linux CI и exact CI/CD обязательны. Фактический SSH account будущего PG CD также подтвердить до cutover; secrets не читать в logs.

**E-PG-05, PR2 delivery PASS / operator cutover PENDING, 20.09.2026.** #296 merge `c9bb6f434b131673bb2604d50275aeb55cb1c8c6` в 04:27:06 UTC. PR CI 35489043133: 531 tests + 24 subtests PASS без skips, 12 SQLite + 12 PostgreSQL browser cases PASS; native recovery и actual private Compose bootstrap PASS. Main CI 35489174199 и CD 35489254347 / job 106021209999 success (CD 04:29–04:30 UTC). Runtime exact revision, DB readiness backend=sqlite / 3.46.1, auth boundary и HTTP smoke PASS. Backup `/data/backups/release-6yta6uxb/quiz.sqlite3`, USER_STATE_PRESERVED. PR2 ветки удалены после merge/tree comparison/delivery. После CD browser reload сохранил owner session и 7/15 ответов. Operator preflight позже PASS, но prepare/cutover не подтверждены; отдельная проверка сообщает отсутствие `.postgres`. Это не успешный PG deploy.

**E-PG-06, operator stdin regression / local PASS, 20.09.2026.** Branch `codex/postgres-command-input`, base `c9bb6f4`, local diff. Новый synthetic child-process test до исправления: 1 FAIL (child поглощает все последующие команды caller), 2 explicit-input cases PASS. После исправления affected operator/deploy/database/HTTP smoke suite: 69 PASS, включая четыре input cases (implicit EOF, explicit empty/bytes/file stream). Production observation — успешный preflight без следующих markers и `POSTGRES_NOT_PREPARED`; stdout не подтверждает повреждение вставки как отдельную причину. Fix закрывает воспроизведённый дефект независимо от terminal echo. Linux/full recovery CI и новая SQLite delivery ещё PENDING; реальные PG writes не выполнялись. Первое локальное pytest выполнение было заблокировано Windows tempfile permissions; успешная проверка выполнена с разрешённым доступом, без ослабления assertions.

**E-PG-07, stdin fix delivery PASS, 20.09.2026 05:41 UTC.** #297 merge `bc878b806ec83590786b7b8e694d231954c41f2d` (05:38:03 UTC). PR CI 35492066170: 535 tests + 24 subtests PASS; обе browser suites/Cloudflare checks PASS. Main CI 35492172739 и CD 35492264242 / job 106029119400 success. DATABASE_READINESS_OK backend=sqlite / 3.46.1, PWA_AUTH_BOUNDARY_OK 401, HTTP_SMOKE_OK и bot/API exact revision PASS. Browser reload сохранил вход и 7 ответов; собственные ветки удалены после tree comparison. Следующий operator run действительно дошёл до prepare, что подтверждает устранение F-029.

**E-PG-08, storage ownership failure HIGH / local fix PASS, 20.09.2026.** Operator record на `bc878b8`: phase allocated, cluster=null, bot/API остаются running, PG Restarting(1). Диагностика: `.postgres/data` uid0/gid0/0700, `data/18` uid0/gid0/0755, `data/18/docker` uid999/gid0/0700; PG log сообщает permission denied на `/var/lib/postgresql`. Production helper создавал private mount root без chown; прежний CI fixture создавал data с default mode, что не воспроизводило VPS. Branch `codex/postgres-data-ownership`, base `bc878b8`: bounded root ownership handoff/retry, negative guards и production-matching CI fixture. Local affected suite 76 PASS, 1 actual Docker skip (Docker CLI отсутствует); этот test REQUIRED в Linux CI. SQL/domain/frontend не менялись. CI/merge/delivery и operator resume PENDING; source не импортирован, PG writes приложения не запускались.

**E-PG-09, final delivery/cutover PASS, 20.09.2026 06:34 UTC.** #298 merge `51cecf8ea04062bfdb3f7ff8bfd30b2d813e4e39`; PR CI 35493782740 (542 tests + 24 subtests, actual private-mount Compose bootstrap и обе browser suites PASS), main CI 35493897474, CD 35493982300 / job 106033611166 success. Operator root output: preflight PASS; `POSTGRES_PREPARED`; `POSTGRES_CUTOVER_OK` на этом SHA; `.postgres/state.json` phase `complete`, cluster `7687501745789132848`, last_error_type=null, updated_at `2026-09-20T06:34:21Z`. Cutover success требует backup/reconciliation/native PG dump/isolated restore/preserved user-state и version/backend/health gates; source SQLite и backups сохранены. Bot/API используют PostgreSQL. После переключения browser reload сохранил signed-in owner, verified/link state и 7 ответов; resume/переход к вопросу 8 из 15 PASS, новые ответы не отправлялись. F-030 закрыт. Все свои ветки удалены, main чистый. Источник production evidence — вывод оператора и ограниченный browser smoke, прямого SSH агента нет.

## Завершённая Goal — PWA-FIRST-001

**Статус: DONE, 20.09.2026.** G-PWA-01–08 закрыты по code/CI/CD/public browser records и owner-reported acceptance. Пользователь подтвердил первый вход/письмо/login/Telegram linking («Все сделал»), затем quiz/reload/result, logout/recovery/login и установку/запуск с иконки («Вроде все ок»). Устройство/браузер ручной установки UNSET; это не самостоятельно наблюдённая browser trace. PR #290–294 merged/delivered, main `391df11`, runtime/public static `1963e76`; все свои ветки удалены. Встроенная PWA Goal завершена. Исторические E-PWA-01–06 сохраняют свои время и ограничения; новые записи не пересчитывают общий readiness.

**Результат:** владелец открывает PsychologyAtlas в обычном desktop/mobile browser, входит по e-mail/паролю, подтверждённо связывает существующую Telegram identity и проходит quiz с тем же банком и сохранённым состоянием. PWA устанавливается в поддерживаемом браузере, запускается вне Telegram и возобновляет попытку после закрытия клиента/перезапуска API. Контур работает на общем FastAPI backend и текущей SQLite; модель не требует подставных Telegram IDs для web accounts.

**Baseline Goal:** `78edfb0bf1e749cd15366fee146e6916a2a0c708`, origin/main 19.09.2026 после planning #289, CI 35451459318 / source-sync CD 35451491475 PASS. **Текущий delivery candidate:** `1963e76b5b897e72440cdda676737713af164c6a` после PR #290–293, records E-PWA-05. Перед docs-веткой повторно проверены Git/GitHub: один worktree, clean main, open PR нет, main protection/rulesets отсутствуют; ветка/base и оставшиеся gates в checkpoint.

### Scope и трассировка

| Часть | Что входит / наблюдаемый результат | Parent AC и граница |
| --- | --- | --- |
| PWA client | React/TypeScript/Vite; адаптивные экраны входа, тем, настройки quiz, вопроса/feedback и результата; installable manifest/icons/standalone; нет обязательного Telegram SDK/context | AC-FND-04/09, quiz часть FND-01/10; Mini App React migration остаётся позже, поэтому FND-04 целиком не закрывается |
| Owner account | E-mail/password registration для разрешённого owner, verification/recovery через Яндекс 360, login/logout/expired session; не-owner и unverified requests не получают доступ | AC-AUTH-01/02, owner-only часть AUTH-03; public registration/sharing не включены |
| Identity и legacy state | Подтверждение обеих identities; устойчивый internal user/actor, сохранение старых IDs/attempts/snapshots/literature state; conflict/replay не присваивают чужую историю | AC-AUTH-05, существующий quiz/state слой FND-02/06; сложное слияние двух историй вне scope |
| Shared quiz | Общие domain/setup/answer/state правила для PWA/Mini App/classic; только approved темы/вопросы; single/mix/all, count/all, optional difficulty, score/explanation и продолжение попытки | AC-QUIZ-01/02/07, доступная часть QUIZ-03/04/05; adaptive, source certification и knowledge links позже |
| Retry и сохранность | Повторный/конкурентный ответ через разные клиенты учитывается один раз; потеря reply не делает ответ неправильным, сохранённый результат и current step восстанавливаются | Quiz часть AC-FND-07/08/10; durable glossary F-018 и другие learning contours не входят |
| Поставка | Воспроизводимый frontend build/lockfile, CI, Docker/VPS/Nginx HTTPS и auth routing; проверенная revision, identity migration backup/recovery и post-checks; existing Telegram routes продолжают работать | Применимые AC-OPS-01/02/03/05 и E14 regression; PWA hosting не создаёт автоматически новый GitHub Environment |

Указание Parent AC — трассировка, не обещание закрыть целый эпик или более широкий AC. Ни planning PR, ни готовая оболочка не меняют BACKLOG на READY. Текущий registry ниже сохраняет фактическое code state; общий процент не пересчитывается.

### Критерии закрытия Goal (G-PWA)

Это проверки выбранного среза существующих требований, не новые project AC и не новый denominator аудита.

| ID | Условие/действие → ожидаемый результат / проверка |
| --- | --- |
| G-PWA-01 | Fresh desktop/mobile browser без Telegram → доступен понятный login; после входа видны реальные backend темы; production HTTPS build отвечает требованиям установки поддерживаемого browser и standalone launch. Проверка: build/typecheck, manifest/assets и browser/installation smoke; offline обучение не требуется. |
| G-PWA-02 | Разрешённый owner регистрируется, подтверждает e-mail, входит, выходит и восстанавливает доступ → flow работает через Яндекс 360; неверный пароль, unverified/disabled account, истёкший/повторный token и mail failure не открывают доступ. Проверка: auth/mail integration и negative matrix. |
| G-PWA-03 | Authenticated PWA account запрашивает связь с legacy Telegram account → требуется backend-verified proof обоих владельцев; existing quiz/literature data и snapshot provenance сохраняются. Replay/занятая identity/подмена ID/чужой account отклоняются без reassignment/automatic history merge. Проверка: миграция и два synthetic users. |
| G-PWA-04 | Владелец выбирает тему/микс/все и объём → получает соответствующие approved вопросы, проверенный ответ/explanation и итог; empty-content, недоступные/ошибочные параметры показываются контролируемо. Difficulty необязательна; разделы, ещё не реализованные, не маскируются под работающие. Проверка: component/API/E2E happy+negative cases. |
| G-PWA-05 | Ответ подтверждён backend; клиент закрывается или API перезапускается → current attempt/result восстанавливается. Lost reply, double click и тот же вопрос одновременно из PWA/Telegram не удваивают answer/score; конфликтующие choices имеют один сохранённый исход. Проверка: restart, concurrency и network-failure regression. |
| G-PWA-06 | Анонимный/чужой пользователь или CSRF/replayed session обращается к state/answer/linking → доступ закрыт. Logout/expiry/recovery отзывает положенные sessions; пароли, токены, initData и персональные данные не попадают в logs, URL diagnostics или публичный cache. Проверка: API/privacy/session tests и cache review. |
| G-PWA-07 | Identity schema и shared-domain changes применены к legacy DB → IDs, attempts, answers, snapshots, categories и literature сохраняются; повтор/ошибка migration имеют проверенный outcome/recovery. Existing Mini App/classic contracts проходят. Проверка: fixtures + isolated backup restore/reconciliation до production. |
| G-PWA-08 | Все PR прошли актуальные CI/review и merge → PWA/backend опубликованы на установленном target; версия/assets, HTTPS/auth routing и bounded smoke подтверждены. Canonical commands/runbook/config owners/checkpoint обновлены, свои merged branches безопасно удалены. Недоступная required проверка оставляет delivery незавершённой. |

### Решения и границы реализации

- Общая quiz domain logic принимает проверенный internal actor; PWA session и Telegram initData — auth adapters. Бизнес-логика/банк не копируются во второй API или клиент. Сохранить текущие `/miniapp/*` contracts и stable content IDs.
- Web account не аутентифицируется произвольным Telegram ID. Email/Telegram identity mapping и schema migration разрабатываются до изменения DB, с неизменными learning identities и regression на ownership. Никаких synthetic Telegram IDs, обнуления прогресса или автоматического объединения конфликтующих историй.
- PR1 schema decision: `users.id` остаётся learning actor; `telegram_user_id` становится nullable UNIQUE. Legacy values/IDs/связанные строки не изменяются. Versioned migration `identity-v1` выполняется отдельной транзакцией init script после verified backup: copy/rebuild users с сохранением columns/indexes/triggers/autoincrement high-water mark, foreign-key check до commit; ошибка откатывает транзакцию. Runtime requests не перестраивают schema. Отдельные web credentials/session tables и verified identity linking добавляются в PR2, открытых web auth routes в PR1 нет. После появления web users rollback на старый Telegram-only schema не допускается; recovery — остановка и forward-fix по existing runbook.
- Linking требует авторизованной PWA session и одноразового подтверждения со стороны проверенного Telegram владельца. Token binding/expiry/replay protection и поведение conflicts фиксируются до реализации; owner e-mail и старую identity не угадывать.
- Password hashing, server-controlled sessions, protected cookie settings, CSRF/Origin guards, revocation, auth attempt limiting и bounded one-time tokens входят в auth implementation. Числовые TTL/лимиты закреплены в PR2 contract ниже; владелец runtime configuration установлен в operator setup до публичного включения. Credentials не хранятся в localStorage или query configuration.
- PWA доступна только предусмотренному owner. Полный public student/guest signup/sharing и Google OAuth отложены; isolation проверяется на двух synthetic identities даже при закрытом production доступе.
- SQLite — временный runtime этой Goal, PostgreSQL остаётся следующей migration Goal. Не вводить dual-write, дополнительный state store, pgvector или массовый content rollout попутно.
- UI покрывает quiz. Offline cache не должен сохранять приватные API responses/credentials; при отсутствии сети показывается неполученное подтверждение и безопасный retry. Полноценные offline attempts/sync queue не добавляются.

### PR2 auth policy и реализация

- Branch `codex/pwa-owner-auth`, base `ba7f627ccbbccd5243ebbc0c2b8b5cdb28045d22`. PR1 #290 merged/delivered: PR CI 35453054839 (379 tests/24 subtests PASS), main CI 35453130086, CD 35453155256/job 105923711272. 15:53 UTC: backup/isolated restore, USER_STATE_PRESERVED, parity/DB/HTTP/image revision PASS; свои ветки удалены. Runtime bot/API — exact `ba7f627`.
- Additive `auth-v1`: отдельные web_accounts, sessions, одноразовые mail/link tokens и bounded auth rate buckets; `users.id` и существующая история не переписываются. Account до onboarding не имеет actor; пользователь выбирает verified legacy linking либо новый независимый actor. При уже выбранном actor linking к другому отклоняется: слияние историй не поддерживается.
- Registration сначала доказывает владение allowlisted e-mail, только затем владелец задаёт пароль (защита от предварительной регистрации чужим паролем). Argon2id: 64 MiB, 3 iterations, parallelism 1; password 15–128 Unicode characters, без trim/скрытого усечения. Новая dependency оправдана хранением паролей. Неизвестные accounts получают generic ответы и тот же password verification workload.
- Session — случайный 256-bit secret в HttpOnly Secure SameSite=Strict host-only cookie; DB хранит digest. Absolute TTL 7 дней, idle TTL 12 часов; logout отзывает текущую session/link request, recovery — все sessions/link requests. CSRF для authenticated mutations + exact Origin/JSON guard для всех POST. Credentials не попадают в URL/query/localStorage/logs/cache.
- Verify token TTL 1 час, recovery 15 минут, Telegram proof 10 минут; случайные 256-bit значения, DB хранит только digest, consume атомарный. Mail ссылку передаёт fragment, frontend удаляет его из address bar до API вызова. SMTP failure не оставляет действующий выданный token. Лимиты общего owner-only auth контура: login 10/min, mail request 5/hour, token consumption 10/min; не IP-based и не trusting forwarded headers. Hash/SMTP concurrency ограничены; production mail без настроек не имитируется.
- Полный действующий contract/config/recovery: [PWA auth](pwa-auth.md); он включает показ Telegram target в PWA перед explicit complete, session cap 10 и ограничения глобального owner-only limiter.
- Linking: active PWA session выдаёт одноразовый код; private Telegram `/link` + отдельная кнопка подтверждения явно показывают destination e-mail; callback проверяет Telegram sender. PWA затем подтверждает завершение по своей session/CSRF. Expiry, logout, recovery, replay, чужая/занятая identity и конфликт actor отклоняются.
- `PWA_ENABLED=false` по умолчанию: routes отсутствуют. Enabled runtime требует owner/origin/SMTP; HTTPS обязателен, явное dev исключение допускает только loopback HTTP. Новая production конфигурация отложена пользователем; synthetic fixtures используют injected mailer, не bypass авторизации.
- Перед новыми auth routes обновить связанные уязвимые FastAPI/Starlette/python-dotenv pins (F-023), проверить совместимость всей suite и dependency audit; unrelated packages не модернизировать. Validation PR2: auth/mail/session/CSRF/link negative+concurrency/restart tests, preservation/backup of new auth tables, existing Telegram contracts, init/seed/parity и disabled-route deployment smoke.

### PR3 PWA client — решения до реализации

- Branch `codex/pwa-quiz-client`, base `01fa89ef9987669a6ebe8e8deb974e0ed9b735f2`. PR2 #291 merged, PR CI 35454488423 (423 tests/24 subtests), main CI 35454588047 и CD 35454626942/job 105927624692 PASS. 16:21 UTC: backup/restore/user preservation/parity/DB/HTTP/exact bot/API images PASS; PWA_AUTH_BOUNDARY_OK=404 подтверждает default-off. Свои PR2 branches удалены; fresh main clean, protections без изменений.
- Frontend canonical root `pwa/`: React 19.3 / TypeScript 7 / Vite 8, npm lock v3; local Node 22.23.1/npm 12.0.2 проверены. Versions pin/engine compatibility сверены с registry/official docs; фактические commands появятся в package manifest и README. Component suite — Vitest/Testing Library, browser suite — Playwright с temporary SQLite/реальным auth API и synthetic mailer, без Telegram SDK/production accounts.
- UI: login → email proof/password/recovery → явный выбор legacy linking или независимого actor → backend темы/quiz setup → question/feedback → result/resume. Difficulty optional. Никаких работающих на вид заглушек будущих разделов. Mobile layout, keyboard/focus/errors и desktop варианты проверяются.
- API только same-origin `/web`; credentials в cookie, CSRF/token/choice только в памяти. Fragment verification/recovery очищается сразу. Abort/network failure не считается ошибкой ответа; retry использует тот же answer payload, backend возвращает исходный outcome. После неопределённого setup результата сначала восстановить server state, не создавать повторную попытку автоматически.
- Manifest/icons/standalone + service worker для публичной offline-заглушки. Private `/web` requests не кэшируются; offline quiz/sync queue не реализуются. Build/version metadata содержит source SHA и hashes статических assets; production PWA пока не включается, hostname/mail settings отложены пользователем.
- Проверки PR3: typecheck/build, component и real-backend browser happy/error/resume/retry/access/fragment/cache cases, desktop/mobile visual review; Python affected suite и preflight config validation до остановки writers. CI получает воспроизводимый frontend job с pinned tooling/lockfile, synthetic data и artifact identity. Required frontend failures блокируют merge. Отдельный opt-in PWA delivery code/шаблон может следовать PR4; реальные secrets/DNS/Nginx не изменяются.

### Предлагаемые последовательные PR

PR4 code preparation: branch `codex/pwa-release-tooling`, base `46280249451c9271174f90eacacd61c9ad2aea0c`; fresh clean main, single worktree, no open PR, protections unchanged. PR3 #292 merged; PR CI 35456442588 (424 Python/24 subtests, 8 component, 12 browser), main CI 35456559219 и CD 35456616972/job 105932925060 PASS. 16:59 UTC runtime exact `4628024`, config preflight/HTTP/PWA disabled gate PASS; stateful migration N/A для PR3. Own branch удалён после tree equality. Main CI public artifact скачан для локальной проверки release tooling.

До кода PR4: подготовить opt-in static release CLI (verify/stage/activate, SHA-256 manifest, immutable release directories, lock и expected previous revision), Nginx HTTPS/same-origin template и unauthenticated post-check. Никаких вызовов этой процедуры на VPS, новых host files/secrets/DNS или workflow auto-activation. Target root/hostname/TLS остаются UNSET; static root предварительно создаёт оператор и помечает для приложения, CLI не bootstrap-ит host. CI build один раз; при будущей поставке использовать trusted exact main artifact после CI и existing backend CD. Переключение только public static files, SQLite/backend configuration не затрагиваются; старые releases сохраняются, rollback требует проверки совместимости. REQUIRED validation: malformed/tampered/wrong-revision/symlink/path traversal, concurrent/stale activation и recovery tests; actual Nginx template syntax/HTTPS/static/API headers smoke в Linux CI; local missing Linux Nginx явно ограничивает соответствующее Evidence. Canonical commands/owners/preconditions/recovery закрепить в runbook. Этот PR завершает запрошенную подготовку кода; G-PWA-08/real mail/installability остаются отложенным external acceptance.

| Этап | Содержательный результат / безопасное состояние main | Зависимости |
| --- | --- | --- |
| 1. Shared actor/domain и identity data | Выделить минимальную общую quiz boundary, спроектировать и проверить versioned SQLite identity migration; сохранить Telegram behavior и snapshots. Новые web routes не активируются для пользователей без готового auth | Baseline/regression, Q-01/02 concrete schema decision; feature/config gate и preservation plan до кода |
| 2. Owner auth, mail и linking | Implement registration/verification/login/logout/recovery + proof-of-both-identities linking; synthetic mail and ownership tests. Production gate fail-closed, отсутствующая mail/config не включает небезопасный обход | Этап 1, Q-06; real owner/sender configuration до включения |
| 3. PWA quiz | React/TS/Vite package и lockfile, actual screens, installability, shared API state/retry, component/E2E tests; CI build/typecheck/test. Доступный staging/локальный test flow, прежний Mini App не подменяется незавершённым UI | Этапы 1/2; новые canonical commands берутся из созданного package manifest |
| 4. Production delivery и проверка | HTTPS PWA/API routing на выбранном VPS target, pinned build/artifact identity, конфигурация владельца/почты, safe migration/recovery и bounded end-to-end acceptance; документация и primary records | Все G-PWA gates, target/config access, применимые CI/CD rules; никакого destructive/bootstrap по умолчанию |

Разбиение уточняется по связности/риску; следующий PR после delivery предыдущего. Для ранних backend PR применима существующая безопасная VPS процедура; новый target/службы/config требуют явной фиксации preconditions до изменения. Если этап 4 требует недоступных прав, независимые code/tests продолжаются, но Goal не объявляется DONE без delivery.

### Зависимости и delivery gates

| ID | Что нужно установить | Состояние / что блокирует |
| --- | --- | --- |
| PWA-D1 | Owner login e-mail и кто управляет allowlist/bootstrap; подтверждение конкретной legacy identity через пользовательский flow | Owner mailbox и config owner установлены пользователем; значения только runtime. Реальный onboarding и подтверждённый Telegram linking PENDING; чужую/legacy identity не подставлять автоматически |
| PWA-D2 | Яндекс 360 sender/SMTP configuration, secret location/owner, доступ к bounded verification/recovery test на адресе владельца | Sender согласован; источник `smart-life-platform/api`, все пять `RECOVERY_SMTP_*` присутствуют, hostname/sender совпадают с согласованными (operator output). Перенос в quiz `.env` и SMTP TLS authentication PASS E-PWA-05; real verification/recovery PENDING. Исходный проект не изменялся |
| PWA-D3 | PWA hostname, DNS/TLS, Nginx config owner/access и deployment unit | Установлены: `psy.cloud-nodes.net`, VPS `167.86.68.98`, root operator через MobaXterm, dedicated root `/var/www/psychology-atlas`. HTTP challenge и сертификат CN hostname до 18.12.2026 подтверждены E-PWA-05. HTTPS app routing/static activation и public smoke из VPS и независимой сети PASS E-PWA-05. Прямой SSH агента отсутствует |
| PWA-D4 | Session/token policy и versioned identity schema/reconciliation/recovery | RESOLVED для этого среза: PR1 identity-v1, PR2 auth-v1/session/token/SMTP policy и migration/reconciliation tests, production backup/restore/preservation PASS. PostgreSQL target design отдельно |
| PWA-D5 | Новые build/test commands, Node/package manager/lockfile и frontend test runtime | PR3: pwa/package.json + npm lock; Node 22.23.1/npm 12.0.2, canonical commands в README; local build/component/browser checks PASS (E-PWA-03) |
| PWA-D6 | Известные dependency findings F-023 и новые auth/runtime inputs | F-023 закрыт #291: compatible pins + fresh audit/behavior checks; PWA npm lock/full npm audit PASS #292. Другие F-015 inputs не объявлены решёнными |

Первоначальный deployment/config явно согласован и выполняется владельцем через root-сессию MobaXterm; агент готовит проверяемые команды и сверяет public/primary Evidence. Новые GitHub Environments, платные сервисы и изменение общих access policies в этот scope не входят.

**E-PWA-05 / operator setup, 19.09.2026 (PARTIAL):** main/backend `1963e76b5b897e72440cdda676737713af164c6a`, PR #293 merged, main CI 35457495076 и CD 35457573364 PASS. CI проверил 451 Python tests/24 subtests, 8 component, 12 browser и actual Nginx; artifact 10588725714, `pwa-1963e76b5b897e72440cdda676737713af164c6a`, GitHub digest `b3dc013b43b8baae38dcee3d44d0c087ca297857990c1c4dc429003f63fbc061`. Для ручного переноса проверенные 12 public files упакованы без rebuild в tar.gz, transfer SHA-256 `6536c6a03be2495c770ccf91f4131ddfe23d050f0ded49dde93d302a37773847`; это отдельный digest упаковки. Оператор подтвердил archive и activation script SHA-256 `40347a688fd90d17f42838e2e7592b7f21c1e09f4e7a45b7f404605c1402c452`. Сценарий выполнил SMTP TLS authentication без отправки письма, private backups, stage, config validation, bot/API restart с проверкой неизменных images/expected revision/auth boundary, static activation и Nginx reload. Последняя успешная phase `local_https_verified`, record `/root/psychology-pwa-setup-1uibc0u4/record.json`; затем public probe завершился `URLError`, процесс вернул shell prompt. Полный installer не повторять: backend/static уже активированы. Независимые проверки агента в 20:38–20:39 UTC: HTTPS `/build.json` exact revision, `/web/auth/me` = 401 JSON/no-store; existing `scripts/pwa_smoke.py` с User-Agent `PsychologyAtlas-Deployment-Check/1.0` и trusted CI artifact — PASS, все 11 assets/hash/MIME/security headers совпали. Пользователь затем выполнил read-only повтор той же smoke-проверки на VPS: PASS для exact revision и всех 11 assets. URLError не воспроизвёлся, первопричина UNSET; повторной установки и изменения конфигурации не было. Серверный post-check gate закрыт, real verification/recovery и owner linking/quiz/standalone acceptance ещё PENDING. Personal mailbox/credentials не записаны в repo. Main перед подготовкой docs PR совпадал с candidate, open PR нет.

**E-PWA-06 / public browser, 19.09.2026 20:46 UTC (PASS в проверенном scope):** ephemeral Chromium, public static revision `1963e76`, desktop 1440×960 и mobile viewport 390×844. Login/register/recover screens отрисованы, переключение форм работает; page errors и горизонтального overflow нет, screenshots просмотрены. HTTPS secure context, active `/sw.js`, public cache с exact revision; Chromium `Page.getInstallabilityErrors` вернул пустой список. Все form submissions заблокированы диагностикой: реальные registration/mail/account state не менялись. Это подтверждает public anonymous UI и browser installability checks, но не фактическую standalone installation, iOS или authenticated owner journey. Raw JSON/screenshots находятся вне repository.

### Validation Plan

Рабочий каталог — repository root для Python/CI; frontend directory определяется PR3 и фиксируется в README/package scripts. Synthetic users/content и isolated DB; production history не используется как test fixture.

| AC/риск | Проверка / ожидаемый результат | Команда/tool и environment | Этап / обязательность |
| --- | --- | --- | --- |
| G-PWA-03/07, FND-02/06, AUTH-05 | Legacy migration retry/failure, IDs/snapshots/answers/literature equality; cross-user/link conflicts не меняют чужой state | Canonical pytest из README, temporary SQLite + backup/isolated restore; конкретные новые test modules в PR | PR1/2 local+CI, REQUIRED; rehearsal перед stateful CD REQUIRED |
| G-PWA-02/06, AUTH-01/02/03, OPS-05 | Password/verification/recovery/session/CSRF/expiry/rate-limit/mail-error matrix, no secret logging и auth cache leaks | API/auth integration + controlled clock/mail fixtures; local+Linux CI | PR2 и regression PR3/4, REQUIRED; real sender check только после PWA-D1/2 |
| G-PWA-04/05, QUIZ-01/02/07, FND-07/08/10 | Setup→answer→feedback→result/resume; duplicate/conflicting cross-client answer, network loss и backend restart | Pytest domain/API/contract + frontend component/E2E; новые commands из manifest, не source-string-only assertions | PR1/3 local+CI, REQUIRED |
| G-PWA-01/04, FND-04/09 | Typecheck/build и UI состояний; реальные desktop/mobile layouts, keyboard/errors и standalone/installation smoke без Telegram | Frontend scripts REQUIRED после создания; browser checks на synthetic account, manifest/icons/HTTPS assets | PR3/4 REQUIRED; владельческий ad-hoc на физическом телефоне RECOMMENDED, не замена suite |
| G-PWA-07, E14 | Сохранены классический quiz/Mini App routes/setup/answer/privacy и approved-content/history semantics | Affected canonical pytest; полный existing suite при общем DB/auth/domain impact, без лишних повторов после PASS | По impact каждого PR, REQUIRED local+CI |
| G-PWA-08, OPS-01/02/03/05 | Проверенные revision/artifact/config, migration preconditions, health и bounded authenticated flow на разрешённых test data; no direct DB/API exposure | Existing GitHub CI/CD + согласованный PWA deploy unit; source/version/health и post-check primary records | Каждый applicable CD REQUIRED; real mail/owner operations только по установленному target и полномочиям |
| PR1 shared identity/domain | Legacy schema rollback/retry/backup preservation, independent actors, concurrent conflicting answers и Telegram regression | `python -m pytest -q`; `tests/test_identity_schema.py`, `tests/test_quiz_service.py`, affected existing suites; init/seed/parity + compileall | Local/CI REQUIRED; Docker compose local N/A при отсутствии CLI, CI REQUIRED |
| Operator documentation, G-PWA-08 | Target/owners, актуальный порядок setup, статус и remaining gates соответствуют user output и primary records; relative links существуют, runtime secrets отсутствуют | `git diff --check`, review Markdown links и итогового diff; repository root, local docs branch; GitHub CI для итогового PR | Local self-review + applicable CI REQUIRED до docs merge; runtime suites N/A для Markdown-only diff без изменения commands/config/code |

**DoD:** G-PWA-01–08 выполнены в указанном scope и подтверждены подходящими автоматическими checks + применимыми browser/delivery checks; все implementation PR merged/delivered; identity/data сохранены; actual commands/config owners и recovery доступны из repo/primary records. Whole-epic readiness не присваивается за частичный срез. После DoD остановиться; следующая предложенная Goal — PostgreSQL migration, scope отдельно выбирает пользователь.

**Non-goals:** PostgreSQL/pgvector cutover в этой Goal; React rewrite Mini App; перенос glossary/literature UI, durable glossary, repetition/adaptive/mastery/analytics, source/knowledge/search, assignments/practice, OAuth/public students/sharing, offline quiz/sync, новые content batches, массовая чистка docs/code, полная инфраструктурная модернизация.

## Последняя завершённая Goal — PLATFORM-STABILIZATION-001

DONE 19.09.2026 по primary records: #284/#285 foundation, #286 API/privacy, #287 glossary retry, #288 history/publication. Закрыты F-016/017/019/020/021/028 и необходимая часть validation/delivery findings; PWA/PG и F-018 не реализовывались. Final main `bd2a5c59beaf3966539b75ebd23ee76638ca2109`, CI 35449928502, CD 35449955313/job 105915264629: backup/isolated restore, USER_STATE_PRESERVED, CONTENT_PARITY_OK, DB/HTTP smoke и bot/API image revision PASS. Final CI 371 tests + 24 subtests PASS. Свои пять PR branches удалены после merge/tree equivalence/delivery; main чистый. Подробные Evidence E-STAB-01–05 ниже. Это reconciliation при планировании следующей Goal, не новый audit/readiness calculation.

## Аудиторский baseline и готовность

Полный аудит: **2026-09-19**, проверенный `origin/main = c914793946c25de78ac5d966969663639cc61b90` по scope Google Doc revision 8. GitHub/source metadata и диагностика проверены примерно 12:46–13:10 UTC; точные primary record timestamps сохранены в Evidence. Это исторический snapshot кода и прежнего denominator, не оценка готовности revision 10 и не обещание текущего LIVE состояния.

**Обязательный согласованный scope: 6 READY / 67 AC = 9,0%.** Полный каталог с девятью условными AC: **6 / 76 = 7,9%**. Условные AC перечислены в [spec](project-spec.md#граница-обязательного-и-условного-scope), сохранены в registry и не активируют implementation. Нулевой процент эпика означает отсутствие полностью доказанного AC, а не отсутствие полезного кода.

Mandatory statuses: **6 READY, 28 IN_PROGRESS, 32 BACKLOG, 1 BLOCKED**. Каталог: **6 READY, 29 IN_PROGRESS, 40 BACKLOG, 1 BLOCKED**. Partial progress не получает дробного веса. Technical AC учитываются наравне с product AC; проценты эпиков не усредняются.

Предыдущей сопоставимой AC-based аудиторской оценки нет: RESUME и PR #282 явно оставляли readiness UNSET. Исторические «completed/ready» относятся к узким Telegram delivery items, не ко всему сентябрьскому PsychologyAtlas scope. Падение/рост в процентных пунктах относительно них вычислять нельзя.

| Эпик | READY / mandatory AC | Готовность | Conditional AC вне primary denominator |
| --- | ---: | ---: | ---: |
| E01 — Platform/data и клиенты | 0 / 10 | 0,0% | 0 |
| E02 — Sources/provenance | 1 / 8 | 12,5% | 0 |
| E03 — Quiz и качество | 0 / 7 | 0,0% | 0 |
| E04 — Повторение/прогресс | 0 / 5 | 0,0% | 1 |
| E05 — Глоссарий | 0 / 2 | 0,0% | 1 |
| E06 — Knowledge/Obsidian | 0 / 5 | 0,0% | 0 |
| E07 — Поиск/optional RAG | 0 / 2 | 0,0% | 3 |
| E08 — Задания | 0 / 3 | 0,0% | 0 |
| E09 — Литература | 0 / 3 | 0,0% | 2 |
| E10 — Практика | 0 / 4 | 0,0% | 1 |
| E11 — Аккаунты/права | 0 / 4 | 0,0% | 1 |
| E12 — Owner dashboard | 0 / 3 | 0,0% | 0 |
| E13 — Deployment/security/recovery | 0 / 5 | 0,0% | 0 |
| E14 — Telegram compatibility | 5 / 6 | 83,3% | 0 |

## Evidence

**E-PWA-04 (19.09.2026, PR4 worktree `codex/pwa-release-tooling`, base `4628024`):** opt-in verify/stage/atomic-CAS activation и complete public SHA-256/HTTPS/auth-boundary smoke, Nginx template и runbook подготовлены. Local release/smoke suite: 23 PASS, 4 Windows symlink privilege SKIP; эти 4 tests REQUIRED в Linux CI, не замена PASS. Default sandbox pytest temp ACL failure устранён повтором в разрешённом test execution; code не менялся ради среды. Trusted main artifact из CI 35456559219, revision `4628024`, verify PASS. Python compile и diff checks PASS. Nginx local отсутствует; actual TLS/template/proxy smoke REQUIRED в frontend Linux job до artifact upload. Self-review: bounded target namespace/marker, immutable retry, stale/concurrent pointer safety, no host/bootstrap/DB changes, trusted CI preconditions и stop/recovery review выполнен. CI/merge/CD PENDING до push; real public PWA/SMTP/owner acceptance отложены. Raw logs/synthetic fixture results не добавлены в repository.
**E-PWA-03 (19.09.2026, PR3 worktree `codex/pwa-quiz-client`, base `01fa89e`):** independent React PWA login/verify/recover/link/new actor/topics/setup/question/feedback/result/resume, manifest/icons/standalone/public offline shell и fixed same-origin transport реализованы. Typecheck/production build PASS (public JS gzip ≈78.5 kB); 8 component tests PASS; 10 real FastAPI/SQLite Chromium desktop/mobile E2E + 2 keyboard cases PASS. Lost response после server commit, uncertain setup, reload, actual password recovery/revocation, linking, no private cache/storage и mobile layout проверены. Same-tab mail fragment defect найден E2E и исправлен. Desktop/mobile login/setup/question/result screenshots просмотрены; physical install/Safari и real Яндекс SMTP не проверены. Backend affected suite 49 PASS; новый preflight отвергает incomplete enabled PWA до остановки writers. npm audit runtime graph: 0 known advisories на момент проверки. Self-review transport/private-state/cache, generated assets, CI permissions/revision и backward compatibility выполнен; CI/merge/CD PENDING до push. New frontend job является обязательным repository gate; pwa artifact identity = tested SHA + SHA-256 manifest. G-PWA-03/04/05/06/07 code slice READY; G-PWA-01/02 имеют внешний acceptance, G-PWA-08 public delivery отложен. Проценты не пересчитывались.
**E-PWA-02 (19.09.2026, PR2 worktree `codex/pwa-owner-auth`, base `ba7f627`):** implemented default-off owner email/password/verification/recovery, Argon2id, durable cookie sessions/CSRF/revocation/rate limits, proof-of-both-identities Telegram/PWA linking и shared quiz API. Local full suite 419 PASS/24 subtests + 1 Docker CLI skip; final affected suite 72 PASS включает три добавленных web delivery gate cases (совокупно 422 cases PASS). Auth code/mocked TLS/mail, two-user isolation, replay/expiry/concurrency/restart, old/new table preservation и Telegram regression PASS; real Яндекс mailbox, PWA UI/HTTPS publication ещё PENDING по user deferral. FastAPI 0.141.1 / Starlette 1.6.0 / python-dotenv 1.2.3 compatible suite PASS; `pip-audit -r requirements.txt` resolved snapshot — 0 known vulnerabilities, не гарантия отсутствия уязвимостей. F-023 закрыт для candidate code; transitive lock/прочие supply-chain findings остаются. Compileall и fresh auth-v1 init/seed/parity (575 questions/8 topics) PASS. Self-review auth/data/transport/recovery boundaries выполнен; SMTP real send не проверялся. CI/merge/CD PENDING до push. G-PWA-02/03/06/07 backend checks PASS, whole G-PWA с UI/real delivery остаются IN_PROGRESS.

**E-PWA-01 (19.09.2026, PR1 #290 merged/delivered `ba7f627`; primary records в PR2 policy выше; исходные local проверки worktree `codex/pwa-shared-identity`, base `78edfb0`):** `quiz_runner`/`quiz_service` общие для authenticated adapters; Mini App contract и classic imports сохранены. `identity-v1` допускает web-only actors без изменения legacy IDs/data; automated backup restore/preservation, migration retry/failure rollback, unique Telegram identity и sequence/index/trigger checks PASS. Auth/browser/installability ещё не реализованы; G-PWA-03/05/07 IN_PROGRESS, остальные G-PWA BACKLOG. Полная local suite: 373 PASS/24 subtests, 5 failures исправлены (перенесённый mock target, Windows command-length в shell harness); corrective suite 59 PASS/8 subtests. Совокупно 378 cases PASS; Docker CLI local недоступен (1 skip), CI обязан выполнить этот contract. Final affected suite 49 PASS/8 subtests; compileall, Bash syntax, diff check и fresh init/seed (575 questions/8 categories)/parity PASS. Self-review: auth boundary, migration atomicity/preservation и compatibility проверены; код не активирует PWA. CI/merge/CD PENDING до push; их результат не предсказывается.

Аудиторские E-CODE/E-TEST/E-REPRO и связанные записи — на audit baseline. E-STAB records относятся к указанным revision/worktree текущей Goal; они обновляют Evidence затронутых AC, не аудиторские проценты. Диагностические scripts/logs/SQLite/venv находятся вне repo; в план включены результаты и воспроизводимые сценарии, не raw logs.

| ID | Результат | Источник/revision/environment и ограничения |
| --- | --- | --- |
| E-SOURCE | PASS/PARTIAL | SRC-01 полностью прочитан ранее 19.09, modifiedTime повторно подтверждён `2026-09-16T11:07:25.174Z`. Drive search/list подтвердил root ID из spec и 8 непосредственных folders (выдача 8 при лимите 100, next page не возвращена); содержимое всех вложенных sources не проверялось. |
| E-UPDATE-10 | PASS source comparison / не code audit | Google Drive file `1V--6vLsM8qSwtPNFQ9MQDp4mW9wlFNufHxxp07tMP1M`, current revision `10`, modified `2026-09-25T14:10:50.303Z`, сравнен с revision `8` от 16.09 через Drive fetch/revisions; 37 групп текстовых изменений, новый scope разложен в spec D-18. Пользователь подтвердил «проверь изменения документа». Code readiness и production по новым AC этой проверкой не подтверждены. |
| E-CODE | PASS inspection | Все tracked app/scripts/tests/SQL/manifests/workflows и doc inventory; Python bot/FastAPI/SQLite + static HTML; target subsystems сопоставлены с AC. Нет vendored tree или hidden implemented PWA в tracked files. Dynamic wiring/entrypoints учтены. |
| E-TEST | PARTIAL | Python 3.12 isolated venv с requirements + audit-only pytest: **264 passed, 16 failed, 1 skipped, 24 subtests passed**, 14,33s. 14 teardown WinError32 (SQLite handles), 2 NamedTemporaryFile reopen failures. Docker CLI отсутствует — 1 compose test skipped. Global dependency mismatch не повторяется в venv. |
| E-FIXTURE | PASS diagnostic / ограниченно | Только 16 failed tests повторены с внешней Windows fixture adaptation: GC перед teardown и closed temporary file; **16/16 PASS**, assertions и product code не менялись. Synthetic /stats matrix: non-owner private/group owner не читают DB, private owner читает. Это не green исходного suite и не Linux/container test. |
| E-CONTENT | PASS structural / PARTIAL semantic | Compileall, questions/topics/glossary/literature validators, fresh DB init/seed, question-bank parity PASS; 575 approved questions/8 topics, 99 approved glossary entries/8 files, 42 review literature entries/4 непустых из 5 files. Fresh DB integrity/FK/parity clean. Quality: 487 uniquely-longest-correct, 21 severe cues. Full source certification отсутствует. |
| E-REPRO | FAIL behavior | Synthetic SQLite/API: mutable content меняет historic option semantics; approved→draft не снимает DB approval; glossary duplicate next меняет correct index и score; malformed payload → 500 / bool category → 200; raw synthetic Telegram ID попадает в logger. Positive quiz setup matrix single/mix/all × 5/10/15/all — 12 PASS. Чужие/неаутентифицированные API requests в suite отклоняются. |
| E-DEPS | FAIL advisory scan / PARTIAL reachability | pip-audit по requirements: 16 выдаваемых записей, **8 unique advisory IDs** в python-dotenv 1.0.1 и Starlette 0.46.2. Primary upstream advisories просмотрены; duplicate records не посчитаны как отдельные уязвимости. Runtime exploitability не доказана; F-023. |
| E-GH | PASS observation | GitHub CLI Just9120, admin; PR #282 merged. Main protected=false, rulesets=[]; один Environment production без protection/branch rules. Default workflow permission read, approve-PR=false. CI baseline 35443743110 success; PR CI 35443715500 success. Первые 20 recent runs просмотрены (не вся история), attempts=1 в выборке. |
| E-CD | PARTIAL | CD 35443743123/job 105899008788 success: checkout c914793, build/seed/restart=0; это docs sync. Последний проверенный runtime restart: CD 28649314034/job 84963244875, bed2283, 03.07, Running обеих служб. Нет доказанного current image digest/version или restore rehearsal. |
| E-HTTP | PASS/PARTIAL | 19.09: published Mini App HTTP 200 и text совпадает с baseline (SHA256 bytes `32fef6fb0b809844f74e579a61d554fb5a8ee29241ba4d791452e79250457896`); API /miniapp/state без initData → 401; public /healthz → Nginx 404. IAB без Telegram показывает понятное требование /ui, console errors/warnings не замечены. Authenticated/mobile Telegram E2E, backend SHA и internal health не подтверждены. |
| E-STAB-05 | PASS code/delivery | #288, merge bd2a5c59beaf3966539b75ebd23ee76638ca2109; CI 35449928502 (371 tests + 24 subtests), CD 35449955313/job 105915264629 PASS 19.09 14:51 UTC. BACKUP_RESTORE_OK `/data/backups/release-rjdq2v0b/quiz.sqlite3`, USER_STATE_PRESERVED, CONTENT_PARITY_OK, DATABASE_SMOKE_OK, HTTP_SMOKE_OK exact revision; bot image sha256:efe7a7efdab0e3c1e1285afd18cb4eb56d74da6b62cffc4c4cad2e6e2d250a12 и API sha256:08ff45bff2d5d19418ace37d8d03ea83ba196371273cc6a42f84c72b58df6320 Running. Cloudflare main check 392be69a-0e00-4910-89b8-e742cce874cf PASS, static files в PR не менялись. Local worktree от 7a5fdc9: Общая suite: 361 PASS, 7 новых cases выявили несовместимость sqlite.Row в reused diagnostic helpers, 1 Docker skip; исправление проверено всеми 139 affected backend/migration/Telegram tests PASS (8 subtests), затем 43 deployment/parity checks PASS; итоговый approved-only guard и affected quiz clients — 123 tests/8 subtests PASS. Реальные init/seed дважды на отдельной SQLite, 575 approved/8 topics, integrity/FK/parity clean; compileall, validators, actionlint и Bash syntax PASS. New snapshot hashes, legacy provenance, immutable trigger, content mutation/history, API owner/recent feedback и stateful recovery/failure gates покрыты. Docker compose обязателен в Linux CI; локально CLI отсутствует. |
| E-STAB-04 | PASS code/delivery | #287 / 7a5fdc9, CI 35449003870, CD 35449032741/job 105912854988 PASS 19.09 14:32 UTC; exact runtime labels, DB/HTTP smoke PASS. Bot image sha256:640913d36367e6a2263243541389cc045a504903c21c04f427061bb973de6ec9; API sha256:521f9c2bb839e69c54f9ffb19f87af4e1e9b01cabb5beca5dd691da6e6db417a. Published HTML normalized sha256 83b0e695f620d171d3b316c6e49896734320ef307711b824a189661a1a3b4f2e совпал с main, Cloudflare build 9790b083-0908-446b-b7dd-5b881b163cab PASS. Local: 143 tests и 8 subtests PASS; stable options, concurrent/replayed answer/next/restart, stale/wrong owner/invalid token и dedicated/alias routes. Node actual UI helper lost-response/double-tap/stale-reply PASS; IAB local synthetic fixture answer failure → same-choice retry → feedback → result → restart PASS, console warnings/errors нет. Telegram production roundtrip не выполнялся. |
| E-STAB-03 | PASS code/delivery | #286 / 9346ab1; focused 184 + subsequent 123/86 tests PASS, Node actual fetch guard + local IAB legacy chooser/form PASS. CI 35448069825 и CD 35448090503/job 105910416453 PASS 19.09 14:14 UTC, internal health exact SHA/auth401, DB smoke, bot image sha256:8a56f5f0852d28dca210ac30d847cb28bb10c3dd0d56a34e0b91ee58546b6c4f / API sha256:6c53b0a5a04af5d0d8c07e6a8860951ffa280aef2334eef531d1cb300395ca65. Published HTML sha256 normalized 9b148a7bfd38cea45c3ee1209765949adf1445b0bc7aeed167e6e6e4415a6b3e, config 361d2c085b6d0580174714a0081e84f8fafcba0ad6f54c3d41bd66afacd03032, совпали с main. |
| E-STAB-02 | PASS delivery | PR #284/#285; final main 8c53aba, CI 35447211955 и CD 35447236343/job 105908164003 (2026-09-19 13:57 UTC). Backup `/data/backups/release-3tghz4bi/quiz.sqlite3`, isolated restore/integrity/FK и USER_STATE_PRESERVED PASS; DATABASE_SMOKE_OK, HTTP_SMOKE_OK exact SHA. Bot image sha256:8f8a9eb25d12a349973a0ff6d7a9d1c2e8711073b7ed564e674295fd01288c7d; API sha256:91af5e21b06f805f152f5a153353c935797217db1ebbfc34d99a7e98cc11256c; оба Running и label=8c53aba. Это bounded read-only runtime smoke, не Telegram roundtrip/load test. |
| E-STAB-01 | PASS local / delivery PENDING | PR1 worktree от df38320: 303 passed, 1 skipped (Docker отсутствует), 24 subtests passed; behavior tests включают 12 real-shell/fake-boundary deployment paths, exact CI selection, SQLite backup/isolated restore/preservation. 71 affected legacy tests PASS. Эти результаты не подтверждают VPS до post-merge CD. |
| E-DOCS | PASS local | Итоговый spec/plan diff: 76 unique AC/spec-plan parity, 67+9 scope split, 28 unique findings, counts/readiness, relative links/anchors и whitespace PASS; только два разрешённых Markdown files. Source/READY/findings self-review выполнен. CI/merge определять по PR head branch и primary records, не по будущему обещанию в snapshot. |

### Окружения и delivery facts

| Поверхность | Установлено / UNSET / N/A |
| --- | --- |
| Local diagnostic runtime | Windows/Python 3.12/isolated venv, synthetic SQLite; Docker отсутствует, WSL не установлен. Не GitHub Environment. |
| CI | PR main, push main, manual; ubuntu-latest/Python 3.12; contents:read; concurrency workflow/ref с cancel. PR checkout default merge ref; head_sha записи run отличать от фактического test-merge tree. Required GitHub checks/reviews не настроены; published checks ожидаются по AGENTS. |
| production Environment | CD Deploy production job; SSH к VPS checkout /opt/psychology-quiz, services psych_quiz_bot + psych_quiz_miniapp_api. Compose project psychology-quiz подтверждён runtime checks E-STAB-02/03/04; значения Actions secrets не извлекались. В E-PWA-05 владелец отдельно подтвердил target VPS и управление config через root/MobaXterm. Нет environment protections. |
| Application production | Compose loopback ports и ./data bind; source content read-only mount; .env + explicit Compose overrides. Известны имена secrets/vars из procedures, не их values. API через Nginx; internal health и image revision подтверждены E-STAB-02/03/04; внешний firewall и отдельный authenticated roundtrip не проверялись. |
| Static deployment | Отдельный Cloudflare Worker psychology-quiz-miniapp, assets ./miniapp, current published HTML подтверждён. Это deployment unit, отдельный GitHub Environment не обнаружен. |
| Product variants | Classic, Mini App и самостоятельная PWA — клиенты в общей модели данных. PWA client реализован #292; dedicated static deployment на том же VPS подготовлен #293, initial publication выполняется по E-PWA-05. Отдельного GitHub Environment для PWA нет. |
| Queue/artifact/stateful | CD workflow_run successful exact main CI/manual; concurrency без cancel + failing lock timeout, stale SHA отказ. Candidate images строятся до migrations, label/digest подтверждаются; backup/isolated restore E-STAB-02. VPS rebuild inputs не полностью immutable; отдельное restore/retention Q-08 UNSET (F-014/015). |
| Длительность/стоимость | Baseline CI job 11s, CD job 13s; PR CI workflow ~19s. PR+main CI повторяют validators/install, docs push запускает CD. Billing/остаток minutes UNSET; выборка недостаточна для бюджета/latency SLO. Speculative reruns не запускались. |

## Состояние AC

Реестр включает действующие AC revision 10. Затронутые строки обновлены по E-UPDATE-10 и известным PR/Goal records; незатронутые строки могут сохранять прежний audit/Goal baseline и не являются новым полным аудитом. E-FIXTURE подтверждает behavior с явно описанным ограничением; raw suite failures сохранены. Для BLOCKED указано зависимое решение. Conditional scope помечен отдельно и не превращается в обязательный из-за наличия строки. Исключённые D-18 критерии не входят в действующий реестр.

| AC | Статус | Scope | Evidence / оставшаяся работа |
| --- | --- | --- | --- |
| AC-FND-01 | IN_PROGRESS | MANDATORY | Topic registry, 8 категорий и module metadata есть; E-PL-07 подтверждает 37 exact source/lesson edges на live metadata snapshot, но полная навигация платформы и обработка корпуса отсутствуют (F-026). |
| AC-FND-02 | IN_PROGRESS | MANDATORY | #311 доставил durable due/goals/achievements; #312 доставил actor-scoped PWA/Mini App UI, CI SQLite/PostgreSQL и CD PASS (E-PL-03/05). Student PWA accounts подготовлены синтетически, но production gate закрыт по D-20; полный multi-actor contract по всем личным состояниям ещё не сверен. E-PL-08: связанный owner и второй verified Telegram user проверены сквозным read/write API contract для ответов, ошибок, review, progress, goals, achievements и литературы, 37 затронутых SQLite tests PASS; полный PostgreSQL/client contract PENDING. |
| AC-FND-03 | READY | MANDATORY | Python bot/content/analytics и общий FastAPI backend обслуживают quiz, glossary, cases, repetition, progress, goals и achievements; PR1 #311 PostgreSQL/API CI и PR2 local API smoke PASS. Отдельные ещё не реализованные source/knowledge subsystems учитываются своими AC. |
| AC-FND-04 | READY | MANDATORY | PWA и Mini App собираются React/TypeScript/Vite из общих компонентов и общего backend contract; PR2 local typecheck/build, 31 component и 40 desktop/mobile browser tests PASS; PR CI/Cloudflare delivery остаются отдельными gates. |
| AC-FND-05 | READY | MANDATORY | POSTGRES-MIGRATION-001 завершена: #295–298, real PG suites/backup/restore/preservation и production cutover PASS; E-PG-01–09. Runtime PostgreSQL 18.6, source SQLite сохранён. |
| AC-FND-06 | IN_PROGRESS | MANDATORY | SQLite content sync сохраняет versioned attempts, answers/users/literature (E-STAB-05); target indexes/embeddings и остальные learning subsystems ещё не реализованы. |
| AC-FND-07 | IN_PROGRESS | MANDATORY | Quiz shared-domain/replay/concurrency/restart PASS E-PWA-01/02; PR3 PWA lost-reply/reload/server-state browser checks PASS E-PWA-03. E-PL-11 локально восстанавливает captured текст последнего отвеченного вопроса/кейса в PWA/Mini App после reload и изменения опубликованной редакции; 11 Python/9 component tests и обе сборки PASS. Другие целевые learning contours, PostgreSQL CI/merge/CD локального изменения остаются. |
| AC-FND-08 | READY | MANDATORY | Quiz/literature E-PG-09; durable glossary #302 delivered E-LC-03/04. PWA Reading Tracker PR5 E-LC-09: общий actor/service, SQLite/PG, reload/readback/rollback и сохранение прежних IDs. |
| AC-FND-09 | READY | MANDATORY | #301–305 доставили owner PWA quiz/history/errors/glossary/literature/reset; #312 добавил кейсы, repetition/mastery, цели/достижения и stateless demo. Desktop/mobile browser tests на SQLite/PostgreSQL, PR/main CI и exact CD PASS (E-PL-03/05). Student PWA access остаётся DISABLED по D-20; зарегистрированный owner и гость проверены. |
| AC-FND-10 | IN_PROGRESS | MANDATORY | Bot/Mini App/PWA используют общий quiz domain и PostgreSQL actor mapping; #312 доставил verified Telegram users кейсы, glossary, progress/repetition, литературу и цели через общий React Mini App, CI/CD PASS. Локально `app/literature_chat.py` добавляет чтение/изменение статуса литературы в Telegram-чате через общий actor, связанный PWA/Mini App readback и чужой actor проверены на SQLite (E-PL-05). Полный cross-client contract и PostgreSQL CI/merge/CD нового chat adapter PENDING. E-PL-08: связанный owner отвечает через Mini App и читает тот же ответ/ошибку/review/goals/achievements в PWA, другой Telegram actor изолирован; ответ Telegram-чата читается обоими web-клиентами и PWA retry не дублирует запись, 37 затронутых SQLite tests PASS; CI/merge/CD и полный chat/PG contract PENDING. |
| AC-SRC-01 | IN_PROGRESS | MANDATORY | #311 доставил recursive traversal/complete pagination gate; E-PL-04/07: read-only snapshot 26.09 — 92 папки/413 файлов (PDF 230, Docs 180, DOCX 3), 63/63 tracked metadata current. Приватный aggregate report отделяет форматы и source coverage; полная содержательная обработка не подтверждена, F-007. E-PL-10: приватная очередь всех 413 файлов создана и прочитана обратно, 350 вне reviewed registry; содержательная обработка ещё не выполнена. |
| AC-SRC-02 | IN_PROGRESS | MANDATORY | #311 группирует форматы только по явным lesson/topic links; E-PL-07 проверил 37/37 canonical curriculum edges по точной revision/hash, 12 reviewed learning sources без lesson. Связи полного корпуса ещё не внесены/проверены; F-007/026. E-PL-10: path metadata сравнивается с canonical corpus_path; перенос без content revision требует link review, но полная привязка остаётся. |
| AC-SRC-03 | IN_PROGRESS | MANDATORY | #311 различает new/changed/unchanged/missing и processed/pending/conflict; E-PL-07: 63/63 tracked metadata current, 350 inventory files вне registry. Отсутствует сопоставимый предыдущий полный snapshot и end-to-end derivative update; один текущий snapshot не подтверждает историю изменений, F-007/026. E-PL-10: relocation отделена от content change; приватная очередь без previous/processed snapshots честно помечает соответствующие состояния unknown. Реальный derivative update PENDING. |
| AC-SRC-04 | IN_PROGRESS | MANDATORY | 575 questions/99 glossary approved не имеют проверенного per-item Drive revision mapping; validator этого не требует; F-007. |
| AC-SRC-05 | IN_PROGRESS | MANDATORY | PR1 #311 metadata/lesson conflict fail-closed tests PASS; содержательные противоречия между источниками требуют отдельного source review, полный корпус не прочитан; F-007/026. |
| AC-SRC-06 | READY | MANDATORY | E-LC-07/09: bibliography-only допускает только metadata; knowledge review требует доступный learning material. 14 lists/130 записей показывают not_verified, неизвестные поля и спорные metadata без выдуманного содержания. Negative approval tests PASS; полная сертификация legacy — AC-SRC-05/F-007. |
| AC-SRC-07 | IN_PROGRESS | MANDATORY | #304 доставил exact review gate для текущих question/glossary derivatives; PR1 #311 добавил один source-reviewed авторский кейс с immutable fingerprint и approved-only gate, validators/seed/parity PASS. Полная обработка нового корпуса и приватный вывод Obsidian не подтверждены; legacy certification — F-007. |
| AC-SRC-08 | READY | MANDATORY | Нет runtime LLM/editor routes; entrypoints/dependencies и seed filtering/parity tests PASS (E-CODE/E-TEST/E-CONTENT). Ошибка снятия approval отдельно нарушает SRC-07/QUIZ-01. |
| AC-SRC-09 | IN_PROGRESS | MANDATORY | PR1 case API не выдаёт private `source_ref`; PR2 local проверяет negative API/compiled-asset boundary для demo и Telegram, при этом student PWA gate закрыт. Полный route/asset review, CI и production evidence PENDING. E-PL-09: локальные PWA/Mini App builds PASS, в 15 compiled files нет известных Drive IDs/markers; 64 API tests и 16 subtests PASS. Production artifact и полный role/route audit PENDING. |
| AC-QUIZ-01 | READY | MANDATORY | Backend-derived categories + approved-only selection, lifecycle sync и parity gate; mixed-status/demotion/removal/new-attempt tests E-STAB-05. |
| AC-QUIZ-02 | IN_PROGRESS | MANDATORY | #286/E-STAB-03 подтверждает прежний выбор/валидацию; #311 добавил «Кейсы» и `content_kinds`, #312 доставил theory/case UI. Локальные `34bade1`/`0b14fdb` добавили glossary single/mixed/all по темам, `4c0f7a7` проецирует опубликованные термины в общий quiz bank и обеспечивает theory/glossary/case в ограниченном mixed setup. `a769c2f` проверяет linked PWA/Telegram попытку/feedback/actor isolation. Реализация ещё без PostgreSQL CI, merge/CD и полного UI контракта; статус не повышен. |
| AC-QUIZ-03 | READY | MANDATORY | PR1 #311 доставил adaptive selection due/weak/new при сохранении random, deterministic SQLite/PostgreSQL tests PASS; PR2 local показывает выбор в PWA/Mini App, typecheck/browser PASS. PR2 delivery PENDING. |
| AC-QUIZ-04 | READY | MANDATORY | Необязательный UX доставлен #303; D-19 утверждена. Read-only отчёт по первому ответу actor/captured edition, size/Wilson interval и без изменения metadata: SQLite и real PostgreSQL CI PASS, E-QA-04; QUIZ-QUALITY-ANALYTICS-001 доставлена #306/#309/#310. |
| AC-QUIZ-05 | IN_PROGRESS | MANDATORY | Feedback/duplicate recovery и student-safe отсутствие `source_ref` в answer API проверены, включая локальный linked PWA/Telegram term quiz (`a769c2f`). Canonical question validator требует непустое explanation; локальный seed gate дополнительно отклоняет approved theory с пустым пояснением до DB write (targeted SQLite tests PASS). Полная содержательная проверка понятности 575 legacy пояснений и source certification не подтверждены, F-007; прежний knowledge/source navigation исключён. |
| AC-QUIZ-06 | READY | MANDATORY | #306 delivered, E-QA-01/02: все 575 questions/99 terms рассмотрены, exact review ledger, partial/disputed и 3 исправления с regression; это review существующего банка, не полная source certification или аудит будущих кейсов. |
| AC-QUIZ-07 | READY | MANDATORY | 575 legacy вопросов/IDs и предметные темы сохранены; immutable attempt editions выдерживают content edits/reorder/demotion/removal. Legacy uncertainty явно отмечена; E-STAB-05 и rollout procedure. |
| AC-QUIZ-08 | READY | MANDATORY | PR1 #311/E-PL-01: один разрешённый авторский вымышленный кейс первой встречи с условиями, границей неоднозначности и разбором четырёх вариантов; exact review ledger, validators, SQLite/PostgreSQL CI и CD PASS. Клиническая сертификация не заявлена. |
| AC-QUIZ-09 | READY | MANDATORY | PR1 #311 доставил approved case в теме «Кейсы», single/mixed/`case` selection и immutable feedback на SQLite/PostgreSQL; PR2 local показывает кейс/разбор в PWA/Mini App, component/desktop/mobile browser PASS. PR2 delivery PENDING. |
| AC-PROG-01 | READY | MANDATORY | PR1 #311 доставил edition-aware 1/3/7/14 due-list для quiz/glossary из durable ответов, temporal/actor/reset и PostgreSQL CI PASS. PR2 local показывает очередь в PWA/Mini App, component/browser PASS; delivery PENDING. |
| AC-PROG-02 | READY | MANDATORY | Доставленный errors slice сохраняется; PR1 #311 добавил due queue и запуск quiz/glossary на сегодня с event evidence, PostgreSQL CI PASS; PR2 local добавляет empty/UI, component/browser PASS; delivery PENDING. |
| AC-PROG-03 | READY | MANDATORY | Первая history/statistics/daily dynamics поставлена PROGRESS-001; D-17 hierarchy/unmapped history и фильтры в #309/#310 прошли SQLite/PWA и real PostgreSQL CI browser/contract checks, E-QA-04; поставка подтверждена. Освоение не объявляется по короткой истории. |
| AC-PROG-04 | READY | MANDATORY | Immutable edition/answers/score (E-STAB-05), version-aware error training и captured/backfilled distinction доставлены. PR1 #311 добавил переобучение после новой редакции в due/mastery, temporal/edition/actor и PostgreSQL CI PASS; PR2 local client показывает «новая редакция». Delivery PENDING. |
| AC-PROG-05 | READY | MANDATORY | Quiz и glossary reset поставлены #301/302, E-LC-01–04. Preview/cancel, topic/all, чужие данные/другие темы/литература, auth/CSRF, stale/replay/rollback и обе БД проверены. Реальный owner progress не сбрасывался. |
| AC-PROG-06 | READY | CONDITIONAL / ACTIVATED D-21 | Критерий mastery: 3 верных ответа на одну captured редакцию, интервалы 1/3 дня, охват 7 дней; ошибка/новая редакция сбрасывает. PR1 #311 доставил quiz/glossary evaluator/API с temporal/actor/edition и PostgreSQL CI PASS; PR2 local показывает состояние в PWA/Mini App, component/browser PASS. Старые данные без подтверждённой редакции не доказывают освоение; delivery PENDING. |
| AC-GLO-01 | IN_PROGRESS | MANDATORY | Topic quiz и durable actor state доставлены; #312 перенёс UI в общий React Mini App. Локальная ветка: PWA/Mini App single/mixed/all glossary, per-topic snapshots и частичный reset; `4c0f7a7` добавил общую theory/glossary/case попытку, `cbd88b1` объединил term history/mastery, `a769c2f` подтвердил linked PWA/Telegram API и чужой actor на SQLite. Прежние browser/component checks PASS; PostgreSQL CI, merge/CD и итоговый UI/source contract PENDING. |
| AC-GLO-02 | READY | MANDATORY | #311 доставил glossary due queue/selected review/event с PostgreSQL CI; #312 показал очередь в PWA/Mini App. Локальный `cbd88b1` объединяет captured glossary/quiz ответы одной редакции по term ID без дубля в очереди; SQLite temporal/actor/edition regression PASS. PostgreSQL CI/merge/CD для нового кода PENDING; прежний доставленный критерий не ухудшен по доступному Evidence. |
| AC-KNW-01 | BACKLOG | MANDATORY | Нет source-backed summaries/atomic-note pipeline и validators; F-007/026. |
| AC-KNW-03 | BACKLOG | MANDATORY | Нет Obsidian Markdown export/link validation; F-026. |
| AC-KNW-04 | BACKLOG | MANDATORY | Нет обновления Vault с сохранением personal files; F-026. |
| AC-KNW-05 | BACKLOG | MANDATORY | Требуется подтвердить независимость всех клиентов от Obsidian и отсутствие публикации personal Vault через student routes/assets; прежний PWA knowledge backend исключён. |
| AC-SRH-01 | BACKLOG | MANDATORY | Нет внутреннего owner/agent keyword/semantic retrieval и negative student access evidence; публичный учебный поиск исключён D-18. |
| AC-SRH-02 | BACKLOG | MANDATORY | Нет PostgreSQL/pgvector/index rebuild; F-026. |
| AC-SRH-03 | BACKLOG | CONDITIONAL | Условный architecture decision; иная vector DB не вводилась, но это не выполнение будущего decision AC. |
| AC-SRH-04 | BACKLOG | CONDITIONAL | Условный scope; RAG отсутствует, Q-05. |
| AC-SRH-05 | BACKLOG | CONDITIONAL | Условный scope; evaluation/tokenomics/production inclusion decision отсутствуют, Q-05. |
| AC-LIT-01 | READY | MANDATORY | #305 delivered: 14 прочитанных источников, 130 associations/114 works/12 topics, module/topic/source/revision/locator, ручной duplicate review, сохранены 42 прежних IDs и personal state. Вложенные библиографии всех лекций не объявлены полностью просмотренными. |
| AC-LIT-02 | BLOCKED | MANDATORY | Legacy per-user API/schema/UI и isolation tests PASS; mapping «отложено» ↔ revisit/skipped и target progress требует Q-04. F-005 больше не блокирует tests. |
| AC-LIT-03 | IN_PROGRESS | MANDATORY | Metadata catalog работает; audio format и легальные outbound references не представлены текущим контрактом; F-012/026. |
| AC-LIT-04 | IN_PROGRESS | CONDITIONAL | Условный scope; legacy priority/reading_level есть, taxonomy target не выбрана (Q-04). |
| AC-LIT-06 | BACKLOG | MANDATORY | Нет личного чек-листа/порядка чтения с явным различением рекомендаций агента и приоритетов преподавателя. |
| AC-LIT-07 | BACKLOG | MANDATORY | Текстовые/аудио outbound links и граница «ссылка ≠ доступ по подписке» не представлены проверенным UI contract. |
| AC-LIT-08 | BLOCKED | MANDATORY | Литрес account sync — согласованный intent, но зависит от проверки provider access, цены, данных и способа подключения Q-10; без него ручной прогресс сохраняется. |
| AC-LIT-09 | BLOCKED | CONDITIONAL | Яндекс Книги account sync имеет неподтверждённую допустимость/реализуемость Q-10; без него ручной прогресс сохраняется. |
| AC-PRC-01 | BACKLOG | MANDATORY | Нет case schema с разделением student/client context и corpus criteria; F-026. |
| AC-PRC-02 | BACKLOG | MANDATORY | Нет structured case export; F-026. |
| AC-PRC-03 | BACKLOG | MANDATORY | Нет role package с ограничениями подсказок; F-026. |
| AC-PRC-04 | BACKLOG | MANDATORY | Нет отдельного transcript-analysis package и checks учебной формулировки; F-026. |
| AC-PRC-05 | BACKLOG | CONDITIONAL | Условная voice-практика относится к внешней модели; встроенный API/import transcript исключены D-18. |
| AC-AUTH-01 | IN_PROGRESS | MANDATORY | #291/292 подтверждают owner e-mail/password/verification/login; #312 CI проверил synthetic non-owner account/password flow. Production student registration/login fail-closed до Q-09/11 по D-20; student launch не подтверждён. |
| AC-AUTH-02 | READY | MANDATORY | E-PWA-02: one-time verify/recover, atomic consume, Argon2id и SMTP TLS/error handling tests PASS. E-PWA-05: SMTP config/auth подтверждены; 20.09 owner-reported verification/recovery PASS при закрытии PWA-FIRST-001. |
| AC-AUTH-03 | IN_PROGRESS | MANDATORY | Owner web boundary, CSRF/session/recovery и two-user isolation PASS (E-PWA-02); #312 CI/CD доставил verified Telegram users изолированные learning routes без e-mail и запрет student PWA по D-20. Полный owner/source/Vault boundary и Q-09/11 PENDING. |
| AC-AUTH-04 | BACKLOG | CONDITIONAL | Условный scope; Google OAuth/linking не выбран и не реализован, Q-01. |
| AC-AUTH-05 | IN_PROGRESS | MANDATORY | #290–292 подтвердили owner proof-of-both-identities/linking с replay/conflict guards; универсальный student linking и история across clients по revision 10 требуют policy Q-09 и cross-user validation. |
| AC-AUTH-06 | BLOCKED | MANDATORY | #312 доставил три stateless demo-задания по D-26 с negative persistence tests и desktop/mobile browser CI; доступ после student e-mail регистрации остаётся закрытым в production по D-20/Q-09/11. |
| AC-AUTH-07 | BLOCKED | MANDATORY | Правило допуска 18+ и проверяемый access gate не выбраны; Q-09/11. Возраст не выводится из существующего owner login или Telegram ID. |
| AC-OWN-01 | BACKLOG | MANDATORY | Есть скрытая агрегированная /stats; нет source coverage/pipeline dashboard; F-026. |
| AC-OWN-02 | BACKLOG | MANDATORY | Нет source-to-derivative inventory/gap detection по questions/glossary/cases и personal Obsidian; Q-07, F-026. |
| AC-OWN-03 | BACKLOG | MANDATORY | Нового owner dashboard нет; существующий /stats gate READY только в AC-LEG-04; F-026. |
| AC-OPS-01 | IN_PROGRESS | MANDATORY | API привязан к loopback; Mini App static остаётся в Cloudflare. PWA HTTPS/static и same-origin Nginx→API routing подтверждены E-PWA-05; PostgreSQL topology и actual firewall ещё не проверены. |
| AC-OPS-02 | IN_PROGRESS | MANDATORY | Exact main CI→CD gate, runtime revision/image и bounded post-checks подтверждены E-STAB-02–05. #293 добавил PWA build-once CI artifact, immutable stage/activate и hash smoke; operator activation/public smoke — E-PWA-05. Backend rebuild/input lock остаются F-014/015; owner acceptance ещё не завершён. |
| AC-OPS-03 | IN_PROGRESS | MANDATORY | SQLite snapshots/lifecycle и additive retry/preservation checks выполнены (E-STAB-05). E-PWA-01/02 и CD #290/291 подтверждают additive identity/auth migrations с backup/isolated restore и legacy-state preservation. PostgreSQL migration потребует собственных gates в следующей выбранной Goal. |
| AC-OPS-04 | IN_PROGRESS | MANDATORY | Whole-DB isolated restore rehearsal подтверждён E-STAB-02; отдельное восстановление user data относительно rebuildable content, retention/RPO/RTO ещё UNSET (F-014/Q-08). |
| AC-OPS-05 | IN_PROGRESS | MANDATORY | Auth/access и privacy/destination negative tests PASS; F-021/028 закрыты #286/E-STAB-03. Owner web security tests и F-023 fix поставлены #291, E-PWA-02; полный platform/security scope остаётся. |
| AC-LEG-01 | READY | MANDATORY | Classic single/selected_mix/all, counts 5/10/15/all, feedback: E-TEST + 12-case E-REPRO matrix; русские content fixtures/validators PASS. |
| AC-LEG-02 | READY | MANDATORY | Start/help/menu/reading/fallback handlers: E-TEST PASS по поведению; 14 Windows teardown failures изолированы E-FIXTURE. Новый live Telegram smoke не выполнялся. |
| AC-LEG-03 | READY | MANDATORY | #286/E-STAB-03: runner/entrypoint/context/API contracts PASS; IAB legacy form с hostile context остаётся работоспособной; API destination опубликован и защищён. Ограничение authenticated mobile E2E сохранено. |
| AC-LEG-04 | READY | MANDATORY | Code gate private+owner, aggregate renderer/menu review; E-TEST и synthetic owner/non-owner/group matrix E-FIXTURE PASS. |
| AC-LEG-05 | READY | MANDATORY | Polling default, explicit webhook, classic без Mini App, legacy API switch/dedicated runtime: config/update-mode/runner tests PASS (E-TEST). |
| AC-LEG-06 | READY | MANDATORY | JSON IDs/topic registry и m1-q3 сохранены; validators и existing parity fixtures PASS (E-CONTENT/E-TEST). Target PG migration относится к FND-05. |
| AC-GAM-01 | READY | MANDATORY | #311 доставил durable weekly goals по finished attempts/review events/read literature с multi-actor и PostgreSQL CI PASS; #312 доставил PWA/Mini App UI, component/browser CI и CD PASS (E-PL-03/05). |
| AC-GAM-02 | READY | MANDATORY | #311 доставил достижения по captured curriculum topic, двум последовательным неделям и исправленной ошибке той же редакции из review queue, duplicate/replay и SQLite/PostgreSQL CI PASS; #312 доставил PWA/Mini App UI, CI/CD PASS. Локально F-035 исправляет cross-kind glossary↔quiz выдачу без дубля и при несовпадающей редакции, 51 затронутый SQLite test PASS; PostgreSQL CI/merge/CD этой поправки PENDING (E-PL-06). |
| AC-GAM-03 | READY | MANDATORY | #311 проверил gap/replay и чужой actor negative cases; #312 доставил личные достижения без leaderboard/сравнения, component/browser CI и CD PASS. |
| AC-PRIV-01 | BACKLOG | MANDATORY | Data inventory, понятная пользователю цель обработки и процедура удаления аккаунта/личных данных для multi-user scope ещё не определены; Q-11. |
| AC-PRIV-02 | BLOCKED | MANDATORY | До student launch нужны утверждённые основания обработки, политика и отдельные согласия; содержание/UX/legal review UNSET, Q-11. |
| AC-PRIV-03 | BLOCKED | MANDATORY | Допустимость фактического размещения и локализации для регистрации граждан РФ не проверена; data-flow/hosting decision Q-11. |
| AC-PRIV-04 | BLOCKED | MANDATORY | Решение об уведомлении регулятора и внешних передачах до public launch отсутствует; provider/country data-flow review Q-11. |

## Полный реестр findings и blockers

Приоритеты: P1 — integrity/security/delivery foundation или обязательный платформенный gap; P2 — локальный дефект/verification/maintainability; P3 — отложенное улучшение. Finding не разрешает FIX и не добавляет требования. HIGH относится к конкретно указанному evidence, а не ко всему production.

| ID / состояние | Проблема / область | Evidence, влияние, приоритет, confidence | Действие / основание / зависимости |
| --- | --- | --- | --- |
| F-001 / CLOSED | Прежний spec расходился с SRC-01. | PR #282 merged c914793; E-SOURCE/E-GH. P1; HIGH. | DOCUMENT выполнено: E01–E14/76 AC. Остаточные открытые решения — F-010, не скрытое завершение продукта. |
| F-002 / CLOSED | Старые workflow/router и duplicate CI rules. | PR #282, AGENTS/root ci-cd-rules, удалён docs/ai-coding-workflow.md. P1; HIGH. | CONSOLIDATE выполнено; дальнейшие stale supporting sections — F-009/027. |
| F-003 / OPEN | CD не зависит от CI; main и production без protections. | E-GH: protected=false, rulesets=[]; production rules=[], branch policy=null. На audit baseline CD push/manual независимо от CI; #284/#285 ввели trusted exact CI→CD gate (E-STAB-02). Protections отсутствуют по повторной проверке 19.09. P1; HIGH. | Exact revision/gates исправлены в текущей Goal; GitHub-wide branch/environment protections остаются отдельным policy scope. |
| F-004 / CLOSED | CI не запускает behavioral suite; whitespace step без comparison base малоинформативен. | ci.yml: validators/compile/init/seed, нет tests; checkout + git diff --check проверяет чистое дерево, не PR diff. E-GH. P1; HIGH. | FIX выполнен #284: pytest + Docker contract и explicit base..HEAD; E-STAB-02/CI 35447211955 PASS. F-022 закрыт; это не подтверждает все будущие AC. |
| F-005 / CLOSED — audit blocker снят | Глобальные Python packages несовместимы с requirements. | E-TEST: isolated Python 3.12 venv по requirements запускает API suite; глобальные FastAPI/pydantic не менялись. P2; HIGH. | DOCUMENT: использовать isolated env из README. Старый import failure не считать runtime-дефектом и не оставлять blocker AC-LIT-02. |
| F-006 / OPEN | Backend version/health/recovery и прямой VPS доступ не подтверждены. | E-HTTP: static=main, API unauth 401; public /healthz Nginx 404. E-CD checkout sync не доказывает образ. P2; HIGH для ограничения. | E-STAB-02 подтверждает running version/image, internal health, backup rehearsal и safe smoke через CD. E-PWA-05 устанавливает operator/config owner и target; прямой SSH агента отсутствует, операции выполняет владелец через MobaXterm. Это ограничение исполнения и неполное public PWA Evidence, не доказанный outage; завершить operator delivery и post-checks. |
| F-007 / OPEN | Полная source certification прежнего банка/glossary не подтверждена. | AC-SRC-04/05; E-QA-01: review всех 674 выполнен для QUIZ-06; 188 partial, 4 disputed, legacy refs не всегда primary. 482 supported относятся к выбранным фрагментам, не всему corpus. P1; HIGH. | IMPLEMENT адресную сертификацию оставшихся source gaps по item ledger; прямые mappings/revisions для всего published content и политика конфликтов остаются отдельным scope. Review и 3 исправления выполнены; certification не заявлена. |
| F-008 / OPEN | Length cues, смысловые повторы, неоднозначности и неполные определения/примеры банка. | E-QA-01: 487/575 unique-longest, 21 severe cue; 12 ambiguous items; конкретные issues и группы повторов в learning-quality-reviews. Примеры: m1_gp_034, m2_qual_045, focus_group; 71 glossary examples только отсылают к question ID. P2; HIGH для фактов, MEDIUM для педагогической оценки. | FIX адресно по первичным материалам, без искусственного padding/массового удаления истории. 3 подтверждённых исправления выполнены; остальные issues явно отмечены в рамках QUIZ-06, их устранение требует следующего выбранного scope. |
| F-009 / DEFER | Подробные старые audit reports/RFC уже лежат в repo. | Inventory ниже; banners PR #282 снизили stale authority, raw snapshots всё ещё дублируют evidence. P2; HIGH. | CONSOLIDATE/архивировать в отдельной docs Goal по карте; сохранять unresolved IDs. Audit PR меняет только spec/plan. |
| F-010 / OPEN | Открытые решения Q-01–Q-11 после SRC-01 revision 10. | Spec: statistical policy, student 18+/demo/linking, literature mapping/provider feasibility, privacy/hosting/legal gates, source coverage/recovery. E-UPDATE-10; P1 для student launch, HIGH для наличия gaps, legal conclusions UNSET. | DOCUMENT/получить решения до зависимой реализации и публичного доступа; не трактовать неизвестное как разрешение придумать policy или provider contract. |
| F-011 / DEFER | Legacy m1-q3 и прежние optional content follow-ups. | E-CONTENT/tests, сохранённое решение старого плана. P3; HIGH. | DEFER переименование/новые batches/experimental difficulty до выбора scope; stable ID не является мусором. |
| F-012 / OPEN — частично устранено | Bibliography coverage расширена; taxonomy/форматы ещё неполны. | PR5 E-LC-09: 130 записей из 14 списков, 114 works и PWA Reading Tracker; стабильные IDs/private state сохранены. AC-LIT-01 READY; LIT-02/Q-04 и LIT-03 остаются. P2; HIGH. | IMPLEMENT каталог выполнено; DOCUMENT mapping Q-04 до замены legacy reading statuses. Audio/outbound reference и дальнейшие embedded bibliographies — следующий scope. |
| F-013 / OPEN — переаудирован | Незакрытые риски прежнего runner audit и browser coverage. | AUDIT-001–008 reconciled ниже; frontend tests — 32 source-string contracts; E-HTTP browser только без Telegram. P2; HIGH для test gap, MEDIUM для непроверенных UX failures. | FIX runtime DOM/network/concurrency tests в выбранном scope; не объявлять каждый старый риск воспроизведённым багом. |
| F-014 / OPEN | Поставка mutable main, слабые post-checks и stateful/recovery gaps. | deploy.sh: fetch/ff origin/main, lock может выйти 0 без доставки, проверка Running/logs; seed вызывается до rebuild при app/sql изменениях и может использовать старый image. Bootstrap reset --hard/remove-orphans. E-CD. P1; HIGH для кода, runtime applicability PARTIAL. | Core FIX выполнен #284/#285 + PR4: exact trusted SHA/CI gate, build-before-migration, fail-closed lock/target checks, verified backup/isolated restore, user preservation, snapshot/parity/health/image post-checks и forward-fix procedure. E-STAB-02/05. Осталось: build once/promote immutable artifact вместо VPS rebuild, отдельное user-data restore и backup retention policy (Q-08); отдельный scope, не blocker текущей согласованной additive поставки. |
| F-015 / OPEN | Невоспроизводимые transitive deps/build tooling. | requirements без lock; Actions tags, python:3.12-slim и npx wrangler без pinned version; CD permissions наследует repo default read. E-CODE/E-GH. P2; HIGH. | Actions checkout/setup-python и permissions закреплены #284/#285; CI 35456559219 сообщает Node20 Actions runtime deprecation/forced Node24 и предстоящую Ubuntu26 runner migration (19.10.2026), execution PASS; DEFER совместимое обновление Actions/runner в tooling scope. PWA npm graph фиксирует lock. FIX оставшиеся transitive/base image/wrangler lock inputs в tooling Goal; не объявлять фактический CD token write-capable без evidence. |
| F-016 / CLOSED | Исправление question/options искажает историю ответов. | E-REPRO: после upsert OLD CORRECT → NEW WRONG, recorded_correct=1/current_correct=0. schema references mutable content, snapshot/version отсутствует. AC-PROG-04/FND-06/QUIZ-07. P1; HIGH. | FIX выполнен в #288 / bd2a5c5: immutable snapshot v1/hash/provenance, API/classic feedback из attempt edition, additive backfill до seed; regression/migration/user preservation PASS (E-STAB-05). Ранее утраченные редакции не восстановимы; legacy_backfill_current не выдаётся за original. |
| F-017 / CLOSED | Снятие approval не исключает ранее seeded вопрос из выдачи; parity может пропустить проблему. | E-REPRO: approved → draft seed оставляет DB approved. db.upsert_approved_questions фильтрует вход; audit_question_bank относит canonical retired rows к informational без проверки DB status; existing test закрепляет это. AC-QUIZ-01/SRC-07. P1; HIGH. | FIX выполнен в #288 / bd2a5c5: full seed синхронизирует non-approved/removed IDs без удаления rows/history, partial helper не retire unspecified IDs. Serving parity блокирует non-approved-but-serving; E-STAB-05. Зависимость F-016 закрыта в том же diff. |
| F-018 / CLOSED | Glossary state терялся при restart и не разделялся workers. | Устранено G-LC-02, E-LC-03: durable snapshots/answers/result, общая actor lock, новый процесс и concurrent/replay tests на SQLite/PG, chat/Mini App/PWA restore. AC-FND-08/GLO-02. P1; HIGH. | IMPLEMENT #302 delivered, E-LC-04. История сохраняется до явного learning reset; автоматический TTL/retention не вводится. Scheduled repetition GLO-02 остаётся открытой; knowledge links исключены D-18. |
| F-019 / CLOSED | Повторный glossary next меняет варианты уже показанного вопроса. | E-REPRO: тот же term/order, correct index 3→1; прежний отображённый правильный ответ оценивается false. _safe_question вызывается вновь без stable step. AC-GLO-01/FND-07. P1; HIGH. | FIX stable question instance/step token и idempotent answer/next; regression duplicate/retry/cross-request tests. Выполнено: per-session lock и cached step/answer/next/restart; E-STAB-04. In-memory lifetime F-018 отдельно. |
| F-020 / CLOSED | Malformed quiz/glossary payloads вызывают 500; boolean category ID принимается. | E-REPRO: list в question_count/quiz_mode/difficulty и glossary count → 500; category_ids=[true] → 200 для category 1. set-membership до type guard, isinstance(bool,int). AC-QUIZ-02/LEG-03. P2; HIGH. | FIX #286: shared type/range guards, validation до profile/session writes; malformed/unknown categories/oversized IDs regression PASS, E-STAB-03. Auth bypass не обнаружен. |
| F-021 / CLOSED | Логи содержат raw Telegram user IDs вопреки AC-OPS-05. | miniapp_fastapi._log_request, handler_latency, classic handlers; synthetic log check TRUE. Реальные user IDs не извлекались. P2; HIGH. | FIX убрать или согласованно псевдонимизировать IDs; negative logging tests. Выполнено в #286: raw IDs убраны, negative logging tests PASS; E-STAB-03. |
| F-022 / CLOSED | Canonical unittest command неполон; tests непереносимы на Windows. | E-TEST: 281 pytest cases, 264 pass/16 fail/1 skip; 11 pytest-style tests не запускаются unittest (pytest отсутствует requirements). E-FIXTURE: 16/16 после временной fixture adaptation проходят. P2; HIGH. | FIX реализован в PR1 worktree: requirements-dev/pytest, explicit closing и portable fixtures. E-STAB-01/02: Windows suite PASS с одним Docker skip; Linux CI 35446748336/35447211955 включает Docker и весь pytest suite PASS; #284/#285 merged. Discovery/fixtures finding закрыт. |
| F-023 / CLOSED | Известные advisories в resolved requirements. | Audit E-DEPS: 8 advisory IDs в python-dotenv 1.0.1/Starlette 0.46.2, FastAPI pin ограничивал upgrade. PR2 E-PWA-02: fixed compatible pins, full behavior suite PASS и fresh pip-audit — 0 known vulnerabilities. P2; HIGH для dependency snapshot. | FIX merged/delivered #291 (`01fa89e`), main CI 35454588047 и CD 35454626942 PASS. Reachability прежних form/FileResponse/set_key paths не обнаружена, но новые auth inputs требуют fixed versions. Transitive lock/base images остаются F-015; security completeness не заявлена. |
| F-024 / OPEN | Coupling и точный dead duplicate повышают риск локальных изменений. | classic_quiz_handlers 1778 строк с dynamic _main_attr; main 1125, miniapp_api 1069, index.html 2542. _get_classic_reply_state определён идентично на 107/1266, первая binding замещена. Schema helpers дублированы в init_db/db/schema. P2; HIGH. | REFACTOR по границам domain/transport и одному migration owner; REMOVE только подтверждённый shadowed duplicate отдельным scope. Legacy HTTP adapter имеет consumers/tests/flag и не считается dead. |
| F-025 / OPEN | SQLite connection lifecycle и масштабирование не доказаны. | Classic handlers/main используют with get_connection без close (SQLite context завершает transaction); E-FIXTURE показывает Windows file handles до GC. ORDER BY RANDOM; API GET создаёт/обновляет user; JSON читается с диска на запрос. P2; HIGH для lifecycle, MEDIUM для нагрузки. | Explicit closing в handlers/main/entrypoint исправлен PR1 (E-STAB-01); performance/load измерять перед scale/PG migration, не придумывать SLO. Нет доказанного production bottleneck; duplicate indexes оценить перед удалением. |
| F-026 / OPEN | Значительная часть актуального продуктового scope не реализована. | E-UPDATE-10 и E-PL-01/02: owner PWA/PG/quiz/glossary/literature, один авторский case, backend goals/achievements доставлены в #311; PR2 local добавляет client UI и demo, ещё без CI/merge/CD. Mixed glossary questions, полное освоение private source corpus, student PWA 18+/privacy gates, internal Obsidian/search, reading checklist/provider integrations, external practice packages и owner dashboard остаются. Публичная база знаний/assignments исключены. P1; HIGH для описанных gaps. | IMPLEMENT только выбранной PLATFORM-LEARNING-001 и будущими Goals по roadmap и Q-09–11. Student launch по D-20 отключён; прочие conditional AC отдельно. Finding отражает agreed scope, не даёт полномочий на невыбранные функции. |
| F-027 / OPEN | Supporting docs/config examples содержат stale детали и прежний продуктовый scope. | Старые README/.env/runbook/glossary coverage gaps ниже; E-UPDATE-10 отменяет публичный knowledge/homework/term-card reader scope в ряде supporting RFC, тогда как canonical spec теперь D-18. P2; HIGH для рассинхрона документов, конкретные runtime defects из этого не выведены. | DOCUMENT/CONSOLIDATE в отдельной docs Goal: проверить ссылки/тексты README, knowledge/search/assignments/glossary/literature RFC и API runbooks, перенести полезные текущие контракты в canonical destinations; не менять их в UPDATE spec/plan. |
| F-028 / CLOSED | Frontend доверяет api_base_url из unsigned URL context и отправляет туда initData. | index.html: context из query/hash → apiBase без origin allowlist → Authorization/simple_body initData. Backend сам проверяет initData, но destination не закреплён. AC-OPS-05/LEG-03. P1; HIGH для dataflow, MEDIUM для exploitability. | FIX trusted endpoint configuration/allowlist и credential non-disclosure test. Выполнено в #286: publisher-owned config, origin/path/redirect guard и actual-fetch negative tests PASS; E-STAB-03. |
| F-029 / CLOSED | Operator subprocess наследовал stdin и мог поглотить следующие команды Bash heredoc после preflight. | E-PG-06/07: regression на `c9bb6f4`, fixed #297, CI/CD PASS; operator на `bc878b8` доходит до prepare. G-PG-06/07, AC-FND-05. P1; HIGH. | FIX #297: EOF по умолчанию, явные SQL/manifest bytes и dump stream сохраняются; runbook redirects и 4 regression cases. Никаких schema/data изменений для исправления. |
| F-030 / CLOSED | PG18 не стартовал с root-owned 0700 mount root; CI bootstrap fixture не совпадал с production permissions. | E-PG-08/09; G-PG-06/07, AC-FND-05. P1; HIGH. | FIX #298: bounded UID/GID999 handoff, negative guards, actual production-mode CI fixture PASS. Operator prepare/cutover phase complete на 51cecf8 подтверждает исправление; SQLite source и credentials сохранены. |
| F-031 / CLOSED | Existing CD доставлял только bot/API; PWA CI artifact требовал ручной передачи и activation. | Исторический E-PROG-02; исправление #300 на `2c324bd`, CI `35511910042`, CD `35512024023`, trusted artifact `10605496749`, public asset/revision и owner read-only smoke PASS; G-PROG-05, AC-OPS-01/02/05. P1; HIGH. | IMPLEMENT выполнено: verified artifact/digest/SSH, shared-lock stage/backend/atomic activation/public smoke. Ручная загрузка архива больше не нужна. |
| F-032 / CLOSED | Runtime glossary/literature loaders не ограничивают выдачу publication state; новый draft может попасть в UI. | `app/glossary.py:load_glossary_entries`, `app/literature.py:load_literature_items` на dabb280 не фильтруют status. AC-SRC-07; P2, HIGH по коду, наличие опубликованных draft не заявляется. | FIX G-LC-06, E-LC-07: только exact legacy baseline либо approved source-reviewed content; negative loader tests PASS, #304 delivered / E-LC-08. PR5 повторно проверил прежние 42 bibliography entries вместе с новыми: 130 explicit reviewed records; frozen baseline неизменён. |
| F-033 / CLOSED | Добавленный пункт меню вывел установку/выход за границу короткого desktop viewport. | PR5 CI `35530732596`, pwa-client job `106130740808`: кнопка logout outside viewport; локально воспроизведено на 1280×480. FND-09, P2; HIGH. | FIX вертикальная прокрутка fixed sidebar и запрет сжатия её children; regression проверяет установку, развёрнутую подсказку и logout реальным кликом. Local 30 SQLite + 30 PostgreSQL browser tests PASS, E-LC-10; updated-head CI PENDING. |
| F-034 / CLOSED | Literature loader зависел от cwd процесса и не открывал каталог из canonical PWA test launcher. | PR5 первый browser run: FileNotFoundError; module-relative paths, tmp cwd regression и обе DB browser suites PASS, E-LC-09. FND-09/LIT-01, P2; HIGH. | FIX выполнено в PR5; данные каталога и runtime configs не менялись. |
| F-035 / OPEN | Исправление ошибки на одном термине не давало достижение при переходе между старым glossary и shared quiz review. | E-PL-06: red regression 0 вместо 1, код `app/achievements.py` считал форматы раздельно; AC-GAM-02/GLO-02, P2, HIGH. Влияние — личное достижение отсутствует или при наивном объединении могло бы дублироваться; чужое состояние не затронуто. | FIX локально: общая captured history с actor/edition/review-event boundaries, оба направления и отрицательная stale-projection проверка, 51 SQLite tests PASS. PostgreSQL CI/merge/CD и итоговый review PENDING; не закрывать finding до delivery. |

### Reconciliation прежних AUDIT-001–008 (F-013)

| Старый ID | Результат текущей сверки / действие |
| --- | --- |
| AUDIT-001 | PARTIAL: fresh compact setup/backend hydration теперь есть и проверены contracts; legacy runner fallback/context и stale/network DOM scenario полностью не проверены. DEFER closure до runtime tests, не считать старую permanent-setup гипотезу воспроизведённым bug. |
| AUDIT-002 | DEFER UX: при потере API и cache остаётся инструкция повторить /ui; fallback предусмотрен. Recovery без переоткрытия требует отдельного UX решения, не новый обязательный feature. |
| AUDIT-003 | PARTIAL: try/catch/finally/guards появились, но exception/button recovery не проверяется реальным DOM suite. FIX validation gap совместно с AUDIT-005. |
| AUDIT-004 | OPEN: backend frontend_version=ui-polish-v2, HTML ui-polish-v6-visual-cleanup; build_miniapp_url не добавляет обязательный asset version. Published asset сейчас совпал, cache policy не гарантирована. FIX единый version identity при frontend Goal. |
| AUDIT-005 | OPEN: 32 frontend tests проверяют substrings, runtime DOM/Telegram event wiring suite нет. FIX минимальными сценариями по риску. |
| AUDIT-006 | PARTIAL: PR #282 добавил authority banners/canonical routing, но contradictory operational sections остаются (F-027). CONSOLIDATE по карте. |
| AUDIT-007 | Частично закрыто: exact-origin positive/negative FastAPI tests и origin_allowed diagnostics есть. Exact equality сама по себе не дефект и wildcard не нужен; startup config validation/real alternate-origin matrix остаются в F-027. |
| AUDIT-008 | OPEN risk: setup повторно abandons/создаёт session; server idempotency key и concurrent-setup regression отсутствуют. Не доказана потеря answer data; FIX/проверить при stabilization scope. |

### Документы, generated/legacy области и консолидация

Текущий audit PR не меняет перечисленные ниже supporting files. Canonical destination: intent/AC → spec, findings/state → этот план, workflow → AGENTS, pipeline setup rules → root ci-cd-rules, executable commands → existing scripts/README. Все 27 Markdown документов включены в inventory; дополнительных development-rules не создавать.

| Область | Предлагаемое решение, основание, destination и затронутые ссылки |
| --- | --- |
| README, AGENTS, root ci-cd-rules | KEEP routing; README исправить test runner/config pointers в выбранной docs/tooling Goal (F-022/027). AGENTS/ci-cd не переписывать ради wording. Затронуты README commands и ссылки на tests/runbook. |
| spec/plan | KEEP canonical; этот UPDATE PR меняет только эти два файла. Стабильные requirement/AC/finding IDs сохраняются, исключённые AC перечислены в D-18. |
| delivery-plan-archive, ai-delivery-infrastructure-plan | KEEP history/provenance, не второй roadmap. Ссылки README/plan/AGENTS сохранять. |
| miniapp-deployment-qa, question_bank_content_rollout | CONSOLIDATE текущие операции: устранить legacy API enablement ambiguity, не обещать сохранение смысла history простым abandon; destination те же файлы + executable scripts. Затронуты README/AGENTS, runner design, hydration, question-bank docs. |
| miniapp-quiz-runner-design, miniapp_setup_hydration | KEEP current contracts; MERGE stale duplicate state/launch описания в актуальный hydration/API reference, historical части ARCHIVE отдельно. Затронуты README/spec/AGENTS/runbook и frontend contract docs check. |
| literature_runtime_rfc, glossary_literature_contours_rfc, topic_registry_schema_phase1, proposals/refactor-plan-miniapp-bot | KEEP до решения соответствующих Goals; MERGE подтверждённые current contracts в spec/supporting reference, завершённые предложения ARCHIVE. Затронуты cross-links RFC, runbook и plan; не переносить /next/reminders в scope автоматически. |
| literature_source_inventory | KEEP source-list IDs/limitations; обновить corpus coverage после source Goal, статус только в плане. Затронуты literature RFC и plan. |
| content_audit_all_topics, content_audit_fiziologiya_cheloveka, content_audit_module2, content_source_alignment_module2_qualitative | ARCHIVE полные старые reports вне active docs после переноса всех obligations в plan; source review evidence сохранить по revision. Затронуты quality/docs cross-links, spec/plan supporting references. |
| glossary_content_audit, glossary_coverage, glossary_global_quality_alignment | CONSOLIDATE inventory/provenance в один supporting reference после source review; 96 vs 99 исправить по actual JSON. Detailed historical reports ARCHIVE; затронуты glossary engine/RFC/coverage cross-links. |
| glossary_distractor_engine | KEEP algorithm/reference; linked source obligations и tests обновлять в content Goal. Не source certification. |
| question_bank_quality_and_db_parity, question_bank_quality_calibration | KEEP tooling usage/threshold meaning; state/readiness → plan. Исправить retired lifecycle gate по F-017. Ссылки rollout/scripts/tests/quality reports затронуты. |
| audits/2026-05-24-miniapp-runner-audit и 4 audit JSON | Historical outputs, не runtime assets. Quality/parity reports генерируются существующими scripts; calibration changelog/review queue — curated records, полного воспроизводимого generator для queue нет. KEEP до reconciliation; затем ARCHIVE snapshots, оставить нужные fixtures/queue по tooling decision. На queue/calibration JSON ссылаются tests, удаление без consumer check запрещено. |
| Runtime/legacy/code | Legacy HTTP adapter реально gated/configured/tested, не dead. Dynamic _main_attr и bot handler registration проверены. Подтверждённый shadowed duplicate — F-024. Vendored/generated production code отсутствует; .gitkeep без влияния, оснований для отдельной cleanup Goal нет. |

## Roadmap и предлагаемые Goals

Это предложения для последующих Goals по SRC-01 revision 10; текущая активная QUIZ-QUALITY-ANALYTICS-001 ограничена своими тремя AC. Public student launch требует E16/Q-09/11; книжная синхронизация зависит от Q-10. Приоритеты не запускают новые работы автоматически.

| Приоритет / scope | Результат и критерии закрытия | Зависимости / non-goals |
| --- | --- | --- |
| Завершено — PLATFORM-STABILIZATION-001 | #284–288, scoped defects и prerequisites закрыты; E-STAB-02–05 | Не активная Goal; остаточные findings сохранены |
| 1a — DELIVERY-FOUNDATION-001 | F-003/004/014/015/023: CI→merge→CD exact revision, behavioral suite, воспроизводимые deps/build, controlled migration, version+health/business post-checks и безопасный recovery rehearsal. | Явный pipeline/settings scope и config owner/target; Q-08. Non-goals: продакт-функции и необязательный redesign инфраструктуры. Может быть подготовительным PR в выбранной foundation Goal. |
| DONE — PWA-FIRST-001 | Самостоятельный owner PWA quiz, e-mail auth/linking и общий backend/state; исторический первый срез | Студенческий доступ/demo не входили в эту Goal и остаются AC-AUTH-03/05/06/07 |
| После первого PWA — POSTGRES-MIGRATION-001 (DONE) | PostgreSQL cutover с сохранением identities/attempts/progress, rehearsed migration/reconciliation/recovery; AC-FND-05 | D-09/D-13 и Current Goal: scope/DoD G-PG-01–07 закреплены. Optional pgvector/search не включены |
| ACTIVE — PLATFORM-LEARNING-001 | Выбранные E01/E02/E03/E04/E05 плюс AUTH/GAM/PRIV и активированный PROG-06; два самостоятельно поставляемых PR | Scope/DoD/Validation Plan в начале плана; D-20 закрывает student production access, Q-09/11 остаются launch blockers. Прежняя QUIZ-QUALITY-ANALYTICS-001 DONE (#306/#309/#310). |
| Следующий выбор — STUDENT-ACCESS | AC-AUTH-03/05/06/07 и AC-PRIV-01–04: 18+, demo, student registration/linking, политика данных и launch gates | Q-09/11, проверка фактической топологии/прав; не открывать регистрацию до выполнения gates |
| Следующий выбор — CASES-AND-LEARNING | AC-QUIZ-02/05/08/09, AC-GLO-01/02, E04/E15: тематические кейсы, обязательные пояснения, повторение и личные цели/достижения | Source review/publication E02, D-18; без runtime LLM-генерации и публичного рейтинга |
| Следующий выбор — OWNER-SOURCE-TOOLS | E02/E06/E07/E12: recursive inventory, revision provenance, личный Obsidian Vault, внутренний поиск, owner coverage | Q-07, source access/review, owner isolation; без публичного knowledge/search/homework section |
| Следующий выбор — LITERATURE | AC-LIT-02/03/06/07: статусы, порядок/чек-листы, text/audio links; AC-LIT-08/09 только после feasibility | Q-04/10, privacy/provider rights; ручной режим сохраняется |
| Следующий выбор — PRACTICE-EXPORT | E10 structured external-model и transcript-analysis packages для владельца | Source-backed criteria; без встроенного LLM API, хранения transcript и клиентского раздела практики |
| Отдельное решение | 8 conditional AC и прочие optional исторические предложения | Не включать только по наличию в каталоге или findings. |

### Audit quality review

Исторический AUDIT snapshot от 19.09 на тогдашних 14 эпиках/76 AC: tracked source/tests/config, schema/content tooling, auth/logging, docs, GitHub/settings/records и безопасный live smoke. Все прежние F-001–015 сохранены/обновлены; AUDIT-001–008 reconciled без потери неизвестных closures. E-UPDATE-10 изменила scope до 86 действующих AC в 15 эпиках; E08 сохранён только как указатель на исключённый пользовательский контур заданий. Эта секция не является новым аудитом и не пересчитывает READY/проценты.

READY присвоен только 6 узким AC с code+automatic evidence. Наличие FastAPI или UI не закрывает target PWA/platform AC. 0% не означает «ничего нет». Seed fresh DB PASS не исключает update bugs (F-016/017); зелёный CI не заменяет behavioral tests; строковые frontend contracts не равны E2E.

False-positive review: 16 test failures — Windows fixture/lifecycle, не 16 доказанных бизнес-регрессий; dependency scan 8 unique advisories, не 16 независимых exploit; public health 404 не означает неработающий API; strict CORS и intentional legacy fallback не названы security defects; m1-q3/legacy adapter не удаляются как orphan code.

Ограничения/false negatives: нет authenticated Telegram/mobile E2E, VPS shell/образа/restore evidence, нагрузочного измерения, полного recursive Drive corpus semantic review, private provider settings и billing. HIGH confidence — repo inventory и воспроизведённые defects; MEDIUM — полнота UX/security/external integration assessment; current backend artifact и recovery — UNSET. Ни непроверенное, ни отсутствие найденных secrets в узком pattern scan не выдаются за доказанную безопасность.

## Checkpoint и следующий шаг

Текущий implementation checkpoint по PLATFORM-LEARNING-001 — в начале плана. Исторические UPDATE и Goal records ниже не являются очередью текущих действий. Полный AUDIT в этой Goal не запускался; проценты не пересчитывались.

Исторический checkpoint 20.09.2026: PWA-FIRST-001 и POSTGRES-MIGRATION-001 DONE. Main `51cecf8ea04062bfdb3f7ff8bfd30b2d813e4e39`, #298 CI/CD success; operator PostgreSQL cutover phase complete / cluster `7687501745789132848`, bounded signed-in smoke PASS. Source SQLite/backups сохранены. E-PG-09 обновляет исторические pre-cutover checkpoints без повторного deploy.

PROGRESS-001: продуктовый #299 merged/backend delivered на `771a278`, frontend пока `1963e76` (E-PROG-02). Пользователь разрешил включить PWA в existing CD. Branch `codex/pwa-cd`, base `771a278`, один worktree; automation code/Validation Plan/self-review E-PROG-03 PASS, Linux-only cases ожидают CI. D-14 и исходный product scope сохраняются. Следующий шаг — push/PR automation, актуальные checks/merge, main CI/CD, public и owner read-only smoke; затем cleanup обеих своих веток и закрытие Goal. Операторская загрузка архива больше не требуется; при failures действуют прежние stop/forward-fix gates. Проценты не пересчитывались.
