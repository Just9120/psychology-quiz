services:
  psych_quiz_bot:
    build: .
    container_name: psych_quiz_bot
    restart: unless-stopped
    env_file:
      - .env
    command: ["python", "-m", "app.main"]
    volumes:
      - ./data:/data
      - ./content:/app/content:ro
